# 2026-W35  (2026-08-24 – 2026-08-30)

## 2026-08-29

### [design] `plans/golden-rules-migration.md` – staged replacement of `.ai/`
- **What:** Recorded the plan to replace the `.ai/` governance library with three tiers —
  golden-rule markdown checklists per domain, one executing agent per domain, and an
  orchestrator that finds, authors or improves the rules. Split into eight stages (0–7) with a
  validation gate between each, three of which are declared stopping points where the repo is
  coherent and shippable. Stage 1 is the uncontested cleanup on its own; every later stage is
  optional. Branch `feat/golden-rules` cut from `feat/governance-v14-integration`, **not**
  `main` — `main` is 25 commits behind and contains no `.ai/`, no `.claude/agents/`, no
  `.claude/skills/` and only one of the four test files, so a main-based branch would have had
  nothing to migrate.
- **Why:** Three independent reviews were run before committing to the rewrite. The
  design-soundness review found that the first draft's self-improving loop rebuilt a mechanism
  this repo had already tried and dropped — `docs/checklist-system.md` §6 records *confidence
  self-adjustment* as dropped because "the agent that applied a rule would also decide its own
  false-positive rate… No ground truth anywhere in the loop." The draft rebuilt exactly that
  and rewrote rule *text* rather than a score. It also had the strike criterion inverted
  ("struck if it fired with nothing behind it" deletes the rules that work, since a rule that
  prevents failures produces no failures), and deleted `.ai/03-planning/` — the only artifact
  in the repo that reviews a plan — from a system made entirely of plans. The blast-radius
  review found the migration is larger and quieter than assumed: `.ai/` regrows itself from
  three `mkdir(parents=True)` calls in the hook, `run_self_test` prints
  `[ ok ] checklist sources validated` over an empty directory, `test_hook.py:1427` builds its
  corpus by rglobbing `.ai/` and so would pass vacuously, and `SettingsWiring` — the only proof
  a `PreToolUse` hook is registered in both settings files — lives inside the file being
  deleted. All four corrections are carried into the plan.
- **Provenance:** Subagent model mechanics verified against
  `code.claude.com/docs/en/sub-agents.md` (resolution order: `CLAUDE_CODE_SUBAGENT_MODEL` →
  dispatch-time `model` parameter → frontmatter → main conversation; omitted `model:` means
  `inherit`; nesting to depth 3; hooks and CLAUDE.md inherited by subagents). Frontmatter field
  names cross-checked against this repo's own
  `.claude/skills/claude-code-layout/references/frontmatter-by-type.md`, which confirms
  `effort` (`low`…`max`) and camelCase `disallowedTools` for subagents.

### [config] `.claude/agents/stage-gate-auditor.md` – read-only gate certifier
- **What:** New subagent, mirrored byte-identically to `templates/agents/`. `model: opus`,
  `effort: high`, `disallowedTools: Write, Edit, NotebookEdit` — it runs the suite and the
  self-test but cannot change what it judges. Reports **pass / fail / cannot verify** per gate
  criterion with the raw output behind each verdict, and closes with a single
  `PROCEED` / `DO NOT PROCEED` line. Its body names the six failure modes it must actively
  suspect: the vacuous pass over an empty directory, `--self-test` exiting 0 on
  `validation skipped`, a silently shrinking test count, directories that regrow via
  `mkdir(parents=True)`, instruction files pointing at deleted paths (which produce no error at
  all), and unmirrored `.claude/**` edits.
- **Why:** Every gate in the staged plan would otherwise be certified by the same agent that
  did the work — the "no ground truth in the loop" flaw the design review caught, reapplied to
  the implementation process. The auditor is a cheap second opinion, not an authority; the
  go/no-go decision stays with the user.
- **Verification:** Gate 0 requires the auditor to have returned a real failure before it is
  trusted. Deliberate drift was appended to `templates/agents/layout-auditor.md` and the suite
  correctly failed `TemplateMirrors.test_mirrors_exist_and_match` with a byte-level diff naming
  the file; drift then reverted and the suite returned to 251 green. **The auditor itself could
  not be dispatched** — Claude Code scans `.claude/agents/` at session start, so a subagent
  written mid-session is not discoverable until the session restarts. Gate 0 is therefore
  **partially verified**: the broken state is provably detectable, but the auditor's own
  reporting is unexercised and remains an open item before Stage 1 is certified.
- **Baseline:** 251 tests pass at stage 0.

### [design] `golden-rules/` – seeded from two incidents, not from speculation
- **What:** Opened the golden-rules library early, out of stage order, with `README.md` (the
  two-layer contract, the two `Status:` forms, and the sourcing rule) plus two domain files:
  `git/existing-project.md` and `claude-code/configuration.md`. Both are seeded from failures
  that occurred **during this session**, each recorded as a dated incident in the `Sources:`
  line of the group it produced. New group in the git rules — *"Before cutting a branch —
  verify the base, don't assume it"* — with the concrete command
  (`git ls-tree -d --name-only <base> -- <path>`) and the re-cut procedure. New group in the
  Claude Code rules — *"Before depending on what you just created"* — stating that a subagent
  written mid-session is undiscoverable until restart and that `/reload-skills` covers skills,
  not subagents.
- **Why:** The plan had these files written speculatively at stage 2, which the plan itself
  flags as the weak path: `.ai/GROWTH.md` — *"a rule that nobody hit yet is a guess; a rule
  harvested from a real mistake is knowledge."* Two real mistakes arrived before stage 2 did,
  so the library is seeded with knowledge rather than guesses. This is the intended growth
  mechanism running for the first time, and it ran on the author's own errors.
- **Incident 1 (base not verified):** A branch was cut from `main` for a migration whose every
  target file existed only on `feat/governance-v14-integration`, 25 commits ahead — no `.ai/`,
  no `.claude/agents/`, no `.claude/skills/`, one of four test files, and `hooks/`/`skills/`
  still at the repo root. Caught on the first `cat` of a file the plan named, before anything
  was written; branch re-cut from the correct base. Nothing verified that the base contained
  the subject of the work.
- **Incident 2 (artifact not exercised):** `stage-gate-auditor` was written, correctly placed,
  correctly mirrored and green across all 251 tests, then failed to dispatch —
  `Agent type 'stage-gate-auditor' not found`. The agent whose whole purpose is to
  independently certify later stages could not itself be certified, and the gap surfaced only
  at the moment of use.
- **Provenance:** Authority citations are version-pinned per the README's own rule — Pro Git,
  2nd ed. (Chacon & Straub, 2014); Conventional Commits 1.0.0; GitHub Flow (retrieved
  2026-08-29); Claude Code sub-agents docs (retrieved 2026-08-29). Items with neither an
  authority nor an incident are labelled `reasoned from first principles` rather than given a
  citation they do not have.
- **Also observed:** running the test suite rewrites `.ai/00-system/maturity-tracker.json` on
  every invocation — reproduced three times, reverted each time to keep the diff clean. This is
  the self-modifying-artifact behaviour stage 1 removes.

### [test] Gate 0 closed — the auditor was made to fail before it was trusted
- **What:** Dispatched `stage-gate-auditor` against Gate 1 with the deliberately false claim
  that Stage 1 was complete, over a tree carrying injected mirror drift. It checked
  independently, **refused the claim**, and returned `fail` on all four criteria with the raw
  command output behind each. It also caught the unreverted drift artifact, flagged that the
  `golden-rules/` commit was Stage 2 work landing while Stage 1 was untouched, and ran its
  destructive experiments in a scratchpad copy rather than the working tree.
- **Correction it made to the plan:** line 56 claimed `test_hook.py:1427` gives a plain green
  no-op on an empty corpus. Only half true — `test_every_tripwire_actually_trips` does, but
  `test_declared_mode_matches_what_the_guard_returns` carries a count assertion that fires
  (`0 != 4`). The plan is amended; that class has partial protection worth preserving in
  whatever replaces it, and *that* is the recorded reason for its stage-3 deletion.
- **Why it matters:** an auditor that has never returned a failure has not been tested. This
  one now has, against a false claim rather than a cooperative one.

### [code] Stage 1 — safety net added, distribution machinery removed
- **What (item 1, archive):** `.ai/` and `templates/ai-library/` copied verbatim to
  `plans/archive/2026-08-29_ai-library-retirement/` (58 files each, counts verified equal)
  with a README stating what the archive is *for* — raw material for the golden-rules domains
  that replace it, not sentiment.
- **What (item 2, port):** `SettingsWiring`'s four portable tests moved from
  `test_governance_library.py` to `test_claude_layout.py`, plus a new corpus guard. This is the
  only proof a `PreToolUse` hook is registered in **both** settings files; it was living inside
  the module stage 3 deletes. The fifth test (artifacts citing settings must cite the class)
  did not move — it is genuinely about library artifacts and retires with them. Dangling
  docstring at `test_claude_layout.py:156` repointed.
- **What (item 3, corpus guards):** New `CorpusNotEmpty` classes in `test_claude_layout.py` and
  `test_skills.py`, plus an inline guard in `test_hook.TestEnforcementModeMatchesGuard.setUp`.
- **What (items 4–5, deletion and false-pass fixes):** `templates/ai-library/` deleted (58
  files). Maturity ladder, tracker write and `DEFAULT_MATURITY_TRACKER` removed from
  `workflow_hook.py`; `governance.maturity_tracker` removed from `schemas/config_schema.json`
  and both configs in the same commit; `maturity-tracker.json` and its manifest entry deleted
  (`total_items` 6 → 5). `TemplateParity` removed with the reason recorded in place.
  `test_writes_maturity_tracker` **inverted** into
  `test_self_test_does_not_write_to_the_repository`, which compares the file tree before and
  after; `test_low_maturity_still_passes` became `test_a_bare_project_still_passes`.
- **Why the maturity ladder went:** it rewrote a *tracked* file on every `--self-test`, so
  merely running the suite dirtied the working tree — reverted by hand three times in one
  session before it was named. An instrument that mutates what it measures is not one. The
  level was also "reported, never enforced": a number nothing consumed, which reads as a score,
  and a score invites optimising the score.
- **Why the false pass went:** with `.ai/` deleted the self-test still printed
  `[ ok ] checklist sources validated within 180d` and exited 0. The branch could not
  distinguish "all clear" from "no corpus". Now counts what it examined, reports
  `(88 items)` on success and `[warn] no checklist items found` on an empty one.
- **Scoping decision, taken and flagged:** item 4 also called for stripping `confidence_level`,
  `risk_source` and `risk_weight` from the rule JSONs. Not done — those 58 files are deleted
  wholesale in stage 3, so the edit evaporates. Only the parts that outlive stage 3 were done:
  the hook, the schema, the configs, and the template mirror. The fields sit inert until then.
- **Verification:** 251 → **259 tests, all green** (the suite grew, as it must). Each new guard
  proven to fire by emptying its directory in an isolated scratchpad copy — including the
  decisive case where `.claude/agents/` **and** `templates/agents/` are both empty:
  `TemplateMirrors` reports `OK` comparing nothing to nothing, while `CorpusNotEmpty` fails.
  That is the vacuous-pass class demonstrated and closed. `--self-test` shows
  `[ ok ] config valid against schemas/config_schema.json`; running it plus the full suite
  produces **zero** change in `git status`.

### [test] Gate 1 returned DO NOT PROCEED — four findings, all acted on
- **What:** The auditor certified criteria 1, 2 and 4 with raw output, and refused the stage.
  It did not take the 251 baseline on trust: it extracted both trees with `git archive` and
  diffed collected test IDs rather than comparing counts, on the grounds that a net gain can
  hide deletions. Findings, and what each cost:
- **Finding 1 — criterion 3 was mis-scoped, a plan defect.** It demanded **no**
  `checklist sources validated` line at stage 1. Unmeetable: stage 1 *copies* `.ai/` (stage 3
  deletes it), so 88 live items remain and the line is a truthful report. The underlying false
  pass **is** fixed — proven by deleting `.ai/` in an isolated copy, where the self-test now
  prints `[warn] no checklist items found under 05-domains/`. Criterion amended in place with
  the reasoning visible, and the absence assertion **moved to gate 3** rather than dropped.
- **Finding 2 — my own item-4 rationale did not hold.** I had recorded that only the parts
  outliving stage 3 were done. `derive_confidence` and `CONFIDENCE_MATRIX` live in
  `workflow_hook.py`, which is **not** deleted in stage 3, and had **zero callers in the
  hook** — kept alive solely by their own tests. That is precisely the machinery item 4 names.
  Now removed, with `TestConfidenceDerivation` (9 tests) and
  `test_confidence_level_recomputes_from_the_items_own_fields`. Same for the CISA bands:
  `RISK_BANDS`, `RiskTaxonomy` (4 tests), and `risk_source`/`risk_weight` out of
  `REQUIRED_ARTIFACT_KEYS`. `history/FORMAT.md` and its mirror updated — they shipped the
  taxonomy to adopters. `docs/RETROSPECTIVE.md` had already recorded that the CISA bands do
  not transfer to a single-operator repo.
- **Finding 3 — silent references, the failure mode with no error message.**
  `.claude/rules/governance-library.md` is **unconditional**: it loads every session and after
  every `/compact`, and it was instructing the next session to mirror `.ai/` into a directory
  this commit deleted, citing a test class this commit deleted. Byte-identical in
  `templates/rules/`, so adopters received the same false instruction. Rewritten, both copies.
  `repo-conventions.md` likewise on the maturity level.
- **Finding 4 — the four-documents-drift convention was violated.** `CLAUDE.md` requires
  `workflow_hook.py`, `schemas/hook_contract.md`, `GUIDE.md` §7 and the skill to move together.
  The hook changed by ~90 lines and the others did not; `hook_contract.md:306` still documented
  self-test step 5 as rewriting `governance.maturity_tracker`, a config key the same commit
  had removed from the schema. All three updated, and the contract now states
  "`--self-test` writes nothing" as a *contract* backed by the new test.
- **Minor, also fixed:** orphaned `maturity-tracker.json.prov.md`; and
  `test_template_skills_are_present` counted directories, so deleting every `SKILL.md` while
  leaving the folders would have passed it — the same vacuous shape one level down.
- **Criterion 1 amended, and flagged as a conflict of interest:** 251 → 244. The criterion
  said the count must not shrink; it now requires every removed ID to carry a recorded reason,
  checked by diffing collected IDs. This is stricter where it matters and looser only in the
  arithmetic — but it was rewritten by the author whose work it had just failed, so the plan
  records that plainly and invites the user to reinstate the hard floor. All 20 removals are
  authorised by item 4; 13 tests added.
- **Verification:** 244 tests green. `--self-test` reports config valid and writes nothing.

### [docs] Gate 1 re-audit — the enforcement claims I said I had fixed, and had not
- **What:** The auditor passed all four criteria and judged both amendments legitimate, then
  refused the stage again on finding 4. My commit message said "All updated" for `GUIDE.md`;
  `git diff` showed it had changed **one paragraph**. Three sites were left asserting
  enforcement by code the same commit deleted:
  - `GUIDE.md:818–820` — claimed `test_governance_library.py` holds `risk_source`/`risk_weight`
    to the CISA taxonomy and `.ai/` to its `templates/ai-library/` mirror. `RiskTaxonomy` and
    `TemplateParity` were both deleted in that commit, and so was the mirror.
  - `GUIDE.md:856–857` — claimed `confidence_level` is derived by
    `workflow_hook.py::derive_confidence` "and recomputed by test", 60 lines from where both
    had been removed.
  - `GUIDE.md:95`, `:779` — repo trees still listing `templates/ai-library/` and the maturity
    tracker.
- **Why this one stings:** `.claude/rules/governance-library.md` states the cardinal rule —
  *never imply enforcement that does not exist* — and names `GUIDE.md` §13 as its full
  rationale. The violation was introduced **by the commit closing a finding about exactly
  this**, in the document the rule points at. `docs/RETROSPECTIVE.md` already carries
  "a governance library that nothing checked drifted in one commit" as a *recurring* entry.
  This is that entry, again, in the same session that quoted it.
- **Also closed:** `conditional-triggers.md` (+ mirror) still instructed mirroring new rules
  into `templates/ai-library/` — a live instruction in the operating-manual skill, shipped
  byte-identical to adopters. `CLAUDE.md:76` described the mirror requirement as current.
  `.ai/GROWTH.md` still required `risk_source`/`risk_weight` and documented the CISA bands —
  and the test that used to hold GROWTH.md and the code in step
  (`RiskTaxonomy.test_growth_doc_documents_the_same_taxonomy`) was itself removed, so nothing
  would have caught the drift. GUIDE trees updated for `stage-gate-auditor.md` and
  `test_claude_layout.py`.
- **Corrected in the plan:** my own removal tally said "9 × `TestConfidenceDerivation`" and
  summed to 19, not 20. The real figure is 10. Flagged by the auditor with the right argument:
  the case for replacing a count floor with a hand-read list rests on the list being right.
- **Open, and deliberately not closed here:** `manifest.json` bookkeeping and `total_items`
  remain (item 4's other half), still enforced by `test_total_items_matches_item_count`. Same
  deferral reasoning as the JSON fields — those manifests retire in stage 3. Recorded so the
  gap is visible rather than assumed closed.
- **Reserved for the user:** whether replacing criterion 1's hard count floor with the ID diff
  is acceptable. The auditor judged it legitimate but explicitly returned the decision, since
  the amendment was written by the author whose work it failed.

### [test] Third audit: stop fixing the sites named, start sweeping — and add the missing test
- **What:** The auditor passed all four criteria a second time and refused the stage a third
  time. Two of its findings were **new false claims introduced by the previous remediation**:
  `.ai/GROWTH.md` said `history/FORMAT.md` "has always said so" (that file records the
  opposite about itself, three lines from the cited passage), and a comment in
  `test_governance_library.py` claimed two fields left the codebase "in the same commit" —
  `git log -S` shows they left one commit and twelve minutes apart. They drifted, and nothing
  caught it, which was precisely the comment's own subject.
- **The pattern, named:** three consecutive commits closing an enforcement-claim finding each
  carried a fresh one. The cause was method, not attention — I was fixing the sites the
  auditor named rather than sweeping for the class. A full sweep found **13 more files** still
  describing deleted machinery as live, including `.ai/README.md` instructing the reader to
  regenerate a tracker via a behaviour removed *as a defect*, and
  `docs/governance-integration-decision.md` carrying **two `live` claims** (§16.3 maturity
  levels, §22 file templates) for machinery this stage deleted.
- **Fixed:** `.ai/GROWTH.md` (3 sites), `.ai/README.md`, both checklist manifests,
  `agent-registry.json`, `governance.yaml`, `docs/governance-integration-decision.md`,
  `docs/checklist-system.md`, `docs/RETROSPECTIVE.md`,
  `docs/claude-code-hook-integration/index.md` (sample output showing a maturity line),
  `ROADMAP.md`, `.github/workflows/tests.yml` (a CI comment explaining why the tree was *not*
  checked for cleanliness — the reason it gave no longer exists), plus dead code in
  `test_governance_library.py`: an unused `workflow_hook` import whose `# noqa` comment was
  false, `TEMPLATE`, and `PARITY_EXEMPT`.
- **The systemic fix — `RetiredMachinery`:** a new test class holding a name-to-permitted-paths
  map for every retired symbol. A mention anywhere else fails the build and names the file;
  the fix is to delete the mention or add the path, so the decision shows in a diff. A second
  test rejects *dead* allowlist entries, so the permission list cannot quietly grow into a
  place a real regression could hide. Historical records (`history/`, `plans/archive/`,
  changelogs, the handover) are exempt by nature — they are supposed to say what used to be
  true. Proven to fire: injecting `derive_confidence` into `README.md` in an isolated copy
  failed with the file named.
- **Why it belongs here:** `resolve_enforcer` already caught the inverse — a rule *claiming*
  an enforcer that does not exist. Nothing caught prose describing a symbol that no longer
  exists, because a stale sentence is not a syntax error. All three occurrences were found by
  a human-facing audit rather than the suite; now the suite finds them.
- **Verification:** 247 tests green (244 + 3). Self-test reports config valid and writes
  nothing. All three `.claude/` → `templates/` mirrors byte-identical.

### [test] Fourth audit: the detector I built to end this pattern did not detect it
- **What:** The auditor passed all four criteria a third time and refused the stage a fourth,
  on the substantive ground that `RetiredMachinery` — added specifically to stop this
  recurrence — caught **none of the three incidents named in its own docstring**. It proved
  this rather than argued it: reintroduced all three verbatim into a clean copy and the suite
  stayed green.
- **Why it failed:** the first version allowlisted whole **files**. Every incident site was
  already on the allowlist for the name it abused — `.ai/GROWTH.md` for `derive_confidence`,
  `GUIDE.md` for `RiskTaxonomy`, `.claude/rules/governance-library.md` for `TemplateParity`.
  So it detected only a retired name surfacing somewhere *new*, while every real failure was a
  false claim inside a file that legitimately discusses the removal. Allowlisting the file
  that gets it wrong is exactly the wrong granularity, and the commit message's "now the suite
  finds them" was false.
- **Redesigned:** the unit is now the **paragraph**, and the question is not "may this file
  mention the name" but "does this passage admit the thing is gone". A paragraph naming
  retired machinery must also carry a removal marker (`removed`, `deleted`, `no longer`,
  `was`, `used to`…). Word-boundary matched, so `TemplateParity` no longer fires inside the
  live class `SkillTemplateParity` — substring matching produced exactly that false positive
  and would have trained the next reader to widen the allowlist.
- **Proven, not asserted:** all three incidents reintroduced verbatim into an isolated copy →
  **3/3 caught**, each naming its file and quoting the offending paragraph.
- **Limitation stated in the docstring rather than hidden:** the marker is blind to what the
  marker is *about*. A paragraph containing an unrelated past-tense phrase alongside a live
  false claim passes — which is how `docs/governance-integration-decision.md` §11 escaped and
  had to be found by hand. It is a floor, not a proof; a green run is not "no stale claims".
- **Also fixed from the audit:** `docs/governance-integration-decision.md` carried a **third**
  `live` claim (§11, five-risk taxonomy, naming deleted `RiskTaxonomy`) — the ledger had said
  "two", which was wrong; plus §11.3's present-tense description of the band mechanism and
  §11's re-check instruction. `.ai/README.md` said "**Nothing here is generated**", which
  replaced a true statement with a false one: the hook writes
  `.ai/08-behavioral-metrics/loop_detection.jsonl` and `.ai/.workflow_check_date`, and the
  auditor demonstrated it with synthetic events. Docstrings in `test_claude_layout.py` and
  `test_skills.py` that cited the deleted mirror as current enforcement.
- **Scan widened:** `SCANNED_SUFFIXES` gained `.fragment`, `.hooks`, `.txt`, `.cfg`, `.toml`
  and an exact-name list for `.gitignore` — `templates/CLAUDE.md.fragment` is the
  adopter-facing instruction file and the migration plan names it as a stage-3 breakage site,
  yet no suffix glob reached it. `plans/golden-rules-migration.md` was **removed** from the
  wholesale exemptions: it is a live plan amended three times this stage, not a historical
  record, and exempting it removed the guard from the file changing most.
- **Data vs claim:** `risk_source`/`risk_weight` are checked in prose but skipped in
  `.json`/`.yaml`, where they are inert data keys in rule files stage 3 deletes wholesale.
  Without that split the test demands the 58-file churn stage 1 deliberately deferred.
- **Verification:** 247 tests green. `--self-test` reports config valid and writes nothing.
  All three `.claude/` → `templates/` mirrors byte-identical.

### [config] `stage-gate-auditor` was forced into the one construct this repo forbids
- **What:** Added a "How to verify" section directing the auditor to `python -c`,
  `git archive | tar`, or `printf` append instead of heredocs, and to keep commands unchained
  (no `cd` prefix, no `&&`). Mirrored to `templates/agents/` in the same commit.
- **Why:** The agent holds `disallowedTools: Write, Edit, NotebookEdit` by design — it must not
  alter what it judges. But it needs scratch scripts to verify anything, so its only route was
  `cat > file <<'EOF'`, which this repo's own `PreToolUse` guard escalates to `ask` on
  `rule-no-heredoc-stdin`. The guard was right; the agent definition was wrong. Three audit
  runs were lost to this — two to API timeouts on over-long runs, one blocked outright at the
  prompt.
- **Confirmed in passing:** `PreToolUse` hooks **do** fire inside subagents, as
  `code.claude.com/docs/en/sub-agents.md` claims. Observed, not inferred — the guard stopped a
  subagent's heredoc.

### [test] Gate 1 certified PROCEED; both non-blocking findings closed
- **What (finding 1, the extension hole):** The auditor defeated `RetiredMachinery` a second
  time by planting a live `TemplateParity` instruction in `docs/MIRRORING.mdx`. `.mdx` was not
  in `SCANNED_SUFFIXES`, so the file was never opened. Inverted the mechanism from an
  **allow-list of extensions to a skip-list**: binaries and generated files are skipped,
  everything else git tracks is scanned.
- **Why:** an allow-list of extensions fails silently for every extension nobody thought of —
  `.mdx`, `.rst`, `.sh`, `.ps1`, extensionless files — which is the exact shape of the defect
  this class exists to catch. The class was reproducing its own subject.
- **Verification:** replanted the identical `.mdx` in an isolated `git archive` copy; the
  detector now fails naming `docs/MIRRORING.mdx:1`. It then caught a **real** instance: my
  first `printf` had landed in the working repo rather than the scratch copy, and the suite
  flagged it before I noticed. First time this guard has caught an unplanned stale claim.
- **What (finding 2, the count):** The plan's removal paragraph said "13 tests added"; the
  actual figure is **16**. Corrected, with the correction stating plainly that this is the
  *second* arithmetic error in that paragraph — the line above it said "9 ×" and summed to 19.
- **Why it is recorded rather than quietly fixed:** that paragraph's entire argument is that a
  hand-read list of removals is more trustworthy than a test count. It has now been wrong twice
  about its own numbers, which is the strongest available evidence *against* the amendment I
  made to criterion 1. The user is deciding that question and deserves the evidence that cuts
  against me.
- **Gate 1 result:** all four criteria pass, verified independently by the auditor with raw
  output. Removals reconcile name-for-name (20, all authorised by item 4). The three original
  incidents are caught 3/3 by the redesigned detector.
- **Known limitation, disclosed in the class docstring:** the paragraph-marker check is blind
  to what the marker is *about*, so a passage mixing an unrelated past-tense phrase with a live
  false claim still passes. The auditor demonstrated this. It is a floor, not a proof.
- **Open for the user:** criterion 1 originally read "the test count did not shrink". This
  work failed it (251 → 247) and I rewrote it into an ID diff. The auditor judged the
  substitution stronger; the plan reserves the decision, and it remains unmade.
- **Verification:** 247 tests green, clean tree, `--self-test` reports config valid and writes
  nothing.

## 2026-08-30

### [decision] Gate 1 criterion 1 settled — the ID diff stands, and is now taken by a script
- **What:** The open decision carried out of stage 1 is closed. Criterion 1 keeps the
  collected-test-ID diff rather than reverting to the hard count floor of 251. Three conditions
  came with the decision and all three are implemented: the baseline commit `4353d8b` is
  **pinned** into the criterion and into `scripts/test_id_diff.py:DEFAULT_BASELINE`; the diff is
  now a committed, runnable script instead of a hand-read list; and **Gate 3's wording was
  changed to the same criterion**, because it still read "count not shrunk" while gate 1 read as
  an ID diff — two gates asking one question two ways.
- **Why:** The objection that discredited the amendment was that a hand-read list of removals is
  as fallible as the count it replaced — and the paragraph making the case had been wrong twice
  about its own arithmetic. On inspection that is an argument against *reading* the diff, not
  against *taking* it. So the reading was removed from the loop. Reinstating the floor was
  rejected on the merits: stage 1 item 4 **ordered** five pieces of machinery deleted and 20
  tests went with them, so a floor at 251 could only be cleared by writing four padding tests.
- **Why gate 3 was changed now rather than when it fails:** stage 3 deletes
  `tests/test_governance_library.py` outright and removes far more coverage than stage 1 did. A
  count floor there fails by construction, and amending it at the moment it fails is precisely
  the conflict of interest gate 1 already recorded once.
- **Verification:** `python scripts/test_id_diff.py` reproduces the recorded figures exactly —
  baseline `4353d8b` 251 collected, worktree 247, removed 20, added 16. The 20 removals
  reconcile name-for-name with the plan's list. The script `git archive`s the baseline into a
  temp dir and collects each tree in a **separate subprocess** (one process would hand the
  second tree the first tree's already-imported modules out of `sys.modules`), passing
  `start_dir='tests'` with no `top_level_dir` — `tests/` has no `__init__.py`, so naming it as
  the top level raises `ImportError` before anything is collected.
- **Risk:** accountability

### [design] Stage 2 — the markdown format now carries real content, and is tested
- **What:** Implemented stage 2 at reduced scope; three of its six items were already satisfied.
  Written: `golden-rules/AUTHORING-GUIDE.md` (full, from the plan appendix),
  `golden-rules/GROWTH.md` (ported from `.ai/GROWTH.md` v1.4 and trimmed hard),
  `golden-rules/git/new-project.md`, `golden-rules/planning/` (2 files),
  `golden-rules/security/review.md`, `tests/test_golden_rules.py`, plus the conditional-loading
  section of `golden-rules/README.md`. `git/existing-project.md` needed no new rules — it
  already carried both hard-won ones.
- **Conversions:** `.ai/03-planning/` — 5 rules, 31 items — became **two** files, because the
  ≤20-item ceiling forces the split: `problem-and-requirements.md` (18) and
  `risk-and-verification.md` (13). That split was the real test of the format and it held: the
  8 original `group_condition` values collapsed into 10 *when it applies* groups with no item
  losing its condition. `.ai/05-domains/rule-security-review.json` — 12 items, 4 groups — became
  `golden-rules/security/review.md`, the most heavily cited domain and so the representative
  case.
- **Why no `templates/` mirror, recorded as an assumption the user can overrule:**
  `templates/ai-library/` was deleted on 2026-08-29 specifically to end the duplicate-maintenance
  obligation. Re-creating it for the replacement library would undo the one uncontested win of
  stage 1. The mirror principle still binds `.claude/rules|agents|skills`, which is untouched.
- **Why no doc frontmatter under `golden-rules/`:** the format declares "no frontmatter, no ids,
  no schema", and provenance is carried by the `Status:` and `Sources:` lines — which are now
  *enforced*, where the frontmatter would not have been. `--self-test` still reports
  `[ ok ] every doc carries frontmatter`; nothing was weakened to allow this.
- **Verification:** 247 → 274 tests, all green. `--self-test` reports config valid.
- **Risk:** design

### [test] `golden-rules/` had zero test coverage; it now has 27 tests, proven to fail
- **What:** Added `tests/test_golden_rules.py`. Before it, **no test in the suite touched
  `golden-rules/` at all** — the library scheduled to replace `.ai/` was entirely unenforced
  while the library it replaces carried a 959-line test file. Four checks: `Status:` is one of
  exactly two forms with a real non-future date; every `##` group carries exactly one `Sources:`
  line **and that line is version-pinned**; ≤20 checkbox items per file; and the `fable` grep.
- **Scope stated rather than left implicit:** only `golden-rules/<domain>/*.md` are held to the
  full shape. The three top-level prose contracts are held to the `fable` grep and, where they
  carry a stamp at all, to its format. Nothing here checks whether an item is *good*.
- **Proven to fail, twice over.** `NegativeControls` runs each check against a malformed string
  — including a well-formed control, without which a check that rejects everything would look
  like a check that works. Then, end to end: a deliberately malformed file was planted at
  `golden-rules/zz-negative-control/malformed.md` and **4 checks failed naming that path**
  (bad `Status:` form, unpinned `Sources:`, 24 items over a ceiling of 20, `fable` on line 31).
  The plant was then removed and the suite returned to green.
- **Two design decisions worth their own line.** Fenced code blocks are blanked before scanning,
  because `README.md` documents the format by *showing* a specimen — without that the library's
  own documentation fails the checks it documents, and the tempting fix is to weaken the check.
  And `- [x]` counts toward the ceiling as well as `- [ ]`, or a file could sail past the
  ceiling simply by being used.
- **Risk:** accountability

### [doc] The new shape test immediately caught two undated citations in `ratified` files
- **What:** `git/existing-project.md` cited "Incident (recurring, ×2)" and
  `claude-code/configuration.md` cited "Incident —", both with no date. Both were written on
  2026-08-29 and stamped `ratified`. Corrected to `Incident 2026-08-04` and `Incident
  2026-08-06`, the real dates from `docs/RETROSPECTIVE.md`.
- **Why it matters:** this is the first thing the shape test caught that nobody had noticed, and
  it is exactly the citation-defect class the old `source_version` field was credited with
  catching. The new format did not lose that capability; it kept it with a check that fires.
- **Consequence applied:** per the format's own rule that *any edit to a golden base resets the
  stamp*, both files moved from `ratified 2026-08-29` to `drafted by orchestrator 2026-08-30,
  unreviewed`. Everything authored in this session carries the same unreviewed stamp, because it
  is true: no human has read this version. Stamping today's work `ratified` would have been the
  format's first lie.
- **Risk:** accountability

### [data] Three things measured by running the system, not by reading it
- **`source_version` was degenerate in 104 of 119 items** — a byte-identical copy of `source`.
  The sharper figure is that **50 of 119 carried no version token at all**: "The Pragmatic
  Programmer, Hunt & Thomas", "Google SRE Book", "The Twelve-Factor App". The two numbers differ
  because a duplicated string can still be a pin — `OWASP ASVS 5.0.0` sits in both fields and is
  perfectly good. Whole domains were duplicated 8/8, 10/10, 12/12; the lone exception is
  `rule-skill-authoring-review.json` at 0/8, which cites "code.claude.com/docs/en/skills,
  retrieved 2026-08-06 (Claude Code v2.1.221)".
  **This changes what gate 2 is asking.** Its first criterion assumes the JSON carries pins the
  markdown might lose. Mostly it does not. Conversion cannot lose a pin that 50 items never had,
  and a shape test enforcing a real pin makes the new format **stricter** than the one it
  replaces. That is why the security conversion was editorial work, not transcription — and why
  five ASVS chapter references were written into it and then **removed**: the original items
  never recorded a chapter, and inventing one would have read as more precise than the citation
  is.
- **The hook never revalidates `03-planning/`.** `run_self_test` globs `05-domains/rule-*.json`
  only (`workflow_hook.py:1560`), so `[ ok ] checklist sources validated within 180d (88 items)`
  covers 88 of 119 items. The 31 planning items have never been age-checked by anything. Not a
  stage-2 blocker; recorded because whatever replaces the revalidation loop must not inherit the
  gap.
- **Conditional loading, measured.** Applying `checklist-selection.json` over the live rule
  files: Phase 2 by size — `typo_fix` 9, `small_change` 50, `new_module` 88, `major_refactor`
  88. Phase 2 at `new_module` by context — `deployment` 13, `skill_work` 17, `authentication`
  25, `database_work` 26, `security_audit` 33, `new_api` 34, `code_review` 38. Phase 1 by size —
  `typo_fix` 0, `small_change` 26, `new_module`/`major_refactor` 31. This reproduces the plan's
  "9 items for a typo fix, 34 for a REST API" exactly.
  **The honest answer for the new format**, now written into `README.md`: the *file* is the
  loading unit, which is coarser. A typo fix in a git repo loads ~20 items because there is no
  smaller unit than the file, where the old table loaded 9. Below roughly `small_change` this is
  a **token-budget regression** and is named as one; at `new_module` and above it loads far
  less, because nothing pulls in 88 items at once. What is genuinely lost is *automatic* per-task
  selection, and nothing restores that before stage 5.
- **Risk:** design

### [test] Gate 2 certified PROCEED; the auditor found one real content loss and three of my errors
- **Gate 2 result:** all three criteria pass, verified independently with raw output. Content
  preservation was checked **item-by-item**, not by summary — 18 + 13 = 31 planning items,
  12 security items, every group condition accounted for. The auditor extracted `HEAD` into an
  isolated `git archive` copy, planted its own malformed file, and watched three checks fail
  naming its path; the working repository was never touched.
- **Finding 1, a real content loss, fixed.** Two group conditions were merged under a header
  reading *"When the work is discretionary — it competes with other work and could be
  declined"*. That is a **conjunction where the union was meant**: read strictly,
  mandated-but-competing work lost "is the cost of doing nothing stated?" and non-competing
  discretionary work lost "was do-nothing considered?". Now *"When the work is discretionary,
  **or** when it competes with other work"* — matching the disjunction the neighbouring group
  already used correctly. This is the one finding that would have justified stopping the line.
- **Finding 2, a latent hole in my own check, closed.** `group_problems` took the citation as
  everything from the `Sources:` line to the end of the group, so **a stray year in unrelated
  prose satisfied the version pin**. The auditor demonstrated it: `Sources: Pro Git.` followed
  by a paragraph containing "2026" passed. Narrowed to the citation's own paragraph, with two
  new tests — one for the auditor's exact case, one proving a wrapped continuation line still
  counts. Latent rather than exercised: all 29 live groups carried their pin inside the
  citation, which made this the cheapest possible moment to close it.
- **Finding 3, the pin check catches absence and never wrongness.** The auditor fed it the five
  defect classes gate 2's first criterion names, and **all five passed**: `RFC 7231 (2014)`
  (obsoleted by 9110), `IEEE 1012-2016`, `OWASP ASVS 4.0.3`, `ISO 31000:2009`, and the
  malformed `OWASP ASVS 5.0.0 Level 9`. So the new format is stricter than the old field on
  *presence* — the old mechanical enforcement was a single non-empty assertion at
  `test_governance_library.py:425` — and no better on *currency*. `AUTHORING-GUIDE.md` now says
  this outright, with those five examples, rather than letting an author infer protection that
  is not there.
- **Finding 4, three arithmetic errors of mine.** I wrote "the 8 original `group_condition`
  values collapsed to 10"; there are **11**. And "no item losing its condition" glossed
  `ALT-RAT-05`, whose rule-level `condition` gated it on *choosing between approaches* while it
  now sits under *Always, before a plan is written*. Widening is the safe direction and the item
  is worth asking always, so it is **disclosed, not reverted**. Recorded rather than quietly
  fixed because this plan has now been wrong about its own numbers three times, and each time
  the artifact was right and the sentence describing it was not.
- **Finding 5, my "nothing was weakened" claim was vacuous.** I cited
  `[ ok ] every doc carries frontmatter` as evidence the no-frontmatter assumption cost nothing.
  It proves nothing: `documentation_directories` is `['docs']`, so that check has only ever
  walked `docs/` and `golden-rules/` was never in scope. True, but true vacuously — the exact
  species of false-pass reasoning this repository keeps catching in itself.
- **Two conditions attached to stage 3, now written into its scope.** (a) A **successor to the
  180-day revalidation loop**: `golden-rules/` has none, `Status:` dates are checked for being
  real and non-future but never for age, and stage 3 deletes the existing loop along with
  `.ai/`. The hand-written `**Revalidation due:**` note in `security/review.md` is evidence the
  problem was seen, but prose is not a loop. (b) A **fidelity diff taken before `rm -r .ai/`** —
  the 31/31 and 12/12 mapping is hand-verified and enforced by no test, and once the source is
  deleted the comparison is permanently unrepeatable.
- **Open question surfaced, not closed:** `golden-rules/` is outside every mirror rule, so
  adopters vendoring `workflow-core` get it at a path nothing points them to. Survivable now
  (it is not a Claude-Code-discovered location, so it is reached by explicit path); stage 5 is
  the first thing that will trip on it.
- **Verification:** 276 tests green after the fixes. `--self-test` reports config valid.
- **Risk:** design

### [design] The growth pass I owed — two items promoted, one recorded below threshold
- **What prompted it:** asked directly whether the session's findings had been fed back into the
  checklists. They had not. Five findings were fixed in the artifacts and corrected in the plan,
  and exactly one reached the library — the "the test will not save you here" bullet in
  `AUTHORING-GUIDE.md`. Fixing the instance and not the class is the failure `GROWTH.md`, written
  the same day, exists to prevent, so the pass was run properly rather than argued about.
- **Promoted, both past the two-occurrence threshold**, into
  `golden-rules/planning/risk-and-verification.md`:
  - *"Does the check's **scope actually include the thing it is being cited for** — would it pass
    identically if that thing were absent?"* Three occurrences: `--self-test` printing
    `checklist sources validated` over an empty directory (2026-08-29); the same line now
    reporting 88 items while 119 exist, because it globs one of two folders; and my own citing
    of `[ ok ] every doc carries frontmatter` as evidence about `golden-rules/`, which that
    check has never walked.
  - *"Is every count and measurement in the plan **reproducible by a command**?"* Three
    occurrences, all in the migration plan: "9 ×" summing to 19, "13 added" when it was 16, and
    "8 group conditions" when there were 11. Each was caught only because something recounted
    independently, and the remedy in every case was the same — commit the command, not the
    number.
- **Recorded below threshold, in `AUTHORING-GUIDE.md` rather than as an item:** when merging two
  groups during a conversion, check whether you wrote "and" where "or" was meant. One occurrence,
  so it does not earn promotion — but it happened during the very conversion the guide was
  written for, and it cost two items their coverage. The guide is where authoring mistakes go.
- **Not promoted, deliberately.** The pin-window hole (a stray year outside the citation
  satisfying the version check) is now enforced by `tests/test_golden_rules.py`. *Already
  tool-enforced* is a **strike** criterion in `GROWTH.md`, so writing a checklist item for it
  would have added an item whose only effect is to cost attention.
- **Trialled before keeping, against work I wrote — the two new items fire on this session.**
  Scope: 3 findings, all three real. Counts: 3 findings, all three real, and it also passes on
  the parts done right — the selection figures and the degeneracy figures were produced by
  committed scripts and the gate auditor re-derived every one independently to the same value.
  Items in that group that did **not** newly apply: "can the proposed check actually fail" was
  already satisfied by the planted-file demonstration, and "scheduled as a step" by the gate
  itself.
- **Headroom noted:** `git/existing-project.md` now sits at exactly 20 items, the ceiling. The
  next item added to it forces the split, not a ceiling debate.
- **Verification:** 276 tests green; every file under the ceiling (12, 20, 17, 18, 15, 12).
- **Risk:** design
