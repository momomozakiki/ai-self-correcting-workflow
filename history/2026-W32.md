# 2026-W32  (2026-08-03 – 2026-08-09)

## 2026-08-04

### [code] `main_branch` auto-detection in the Stop hook
- **What:** Closed the "Harden the hook dispatcher" roadmap item. Added
  `detect_main_branch()` + `resolve_main_branch()` to `hooks/workflow_hook.py` and
  wired the latter into `handle_stop()`, replacing the flat
  `stop_hook.main_branch` lookup that defaulted to `"main"`. Resolution order:
  explicit non-empty `stop_hook.main_branch` → `git symbolic-ref --short
  refs/remotes/<remote>/HEAD` (local ref, no network) → optional `git remote show
  <remote>` `HEAD branch:` parse → `"main"`. Three new config keys
  (`main_branch_autodetect` default true, `main_branch_remote` default `origin`,
  `main_branch_probe_remote` default false) in `schemas/config_schema.json`,
  `.claude/workflow_config.json`, and `templates/workflow_config.json`; the two
  configs now omit `main_branch` so autodetect is the dogfooded path. Resolution
  runs *after* the `stop_hook_active`/block-cap short-circuit and memoises into a
  new `main_branch_detected` state key (persisted even on a non-blocking Stop), so
  a session shells out to git at most once. 11 new tests in
  `TestMainBranchDetection` (precedence, blank-value fall-through, custom remote,
  `(unknown)` HEAD, no-network-by-default, memoisation, and two end-to-end Stop
  cases) — suite 27 → 38, all green.
- **Why:** The hardcoded `main` default mis-fires on every repo whose default
  branch is `master`/`trunk`/`develop`: a dirty tree on the real default branch
  looked like a missed Phase-3 closure and produced a spurious commit reminder on
  every close. The roadmap named `git remote show origin`, but that contacts the
  remote — unacceptable as the primary probe inside `Stop`, which runs while the
  user waits for the session to close. The local `refs/remotes/*/HEAD` ref carries
  the same answer for free (written by `git clone`), so it became step 2 and the
  network probe became opt-in. Explicit pins keep winning, so every existing
  adopter config is unaffected.
- **Refs:** `hooks/workflow_hook.py` · `tests/test_hook.py` ·
  `schemas/config_schema.json` · `schemas/hook_contract.md` · `GUIDE.md` §7.2/§7.4
  (v4.7) · `docs/claude-code-hook-integration.md` §3.4/§4/§5.2/§5.3 (v1.12) ·
  `templates/workflow_config.json` · `.claude/workflow_config.json` ·
  `CHANGELOG.md` · `ROADMAP.md` · `python -m unittest discover -s tests` (38 OK)

### [doc] docs/ — consolidated the imported governance framework and cleaned orphans
- **What:** Imported `Self-Growing Checklist Ecosystem` v13 + v14 (3,054 lines,
  ~96% duplicated, neither carrying frontmatter) were committed verbatim first
  (`6470a2c`) so the pre-fold text stays recoverable, then folded into
  `docs/self-growing-checklist-ecosystem/` per GUIDE §6.4: an `index.md` carrying
  the canonical frontmatter, five split children each inside the ≤5,000-token
  Semantic budget, and a sibling `CHANGELOG.md` (`exclude_from_ai: true`) holding
  the v13→v14 delta. v13 retired. Content preservation was verified
  programmatically — 1,265 non-blank lines in, 1,265 out, identical. Also deleted
  the orphaned `docs/Adaptive Self‑Correcting Workflow.md` redirect stub (zero
  inbound links, self-described as "never load for content"), folded
  `docs/claude-code-hook-integration.md` into a folder (its revision table had
  passed the ~8-row threshold) and bumped it to v1.13, and replaced its restated
  state-key list with a pointer to `schemas/hook_contract.md`.
- **Why:** The two flat files were the exact growth vector §6.4 exists to stop,
  and a duplicated state-key list in a doc that already calls `hook_contract.md`
  "authoritative" is drift waiting to happen. The fence-aware slicer was needed
  because two `# ` headings live inside code fences and would have created bogus
  sections.
- **Risk:** accountability — deleting imported source risks losing provenance;
  mitigated by committing the raw files before the fold.
- **Refs:** `docs/self-growing-checklist-ecosystem/` · `docs/claude-code-hook-integration/`
  · `schemas/hook_contract.md` · commit `6470a2c`

### [code] Loop detection, `--self-test`, and the governance library
- **What:** Three v14 controls implemented in `hooks/workflow_hook.py`.
  (1) **Loop detection**: `PostToolUse` hashes `(tool_name, tool_input)` into a
  16-char signature, keeps a rolling 20-entry window in session state, and flags a
  run of consecutive identical calls at `loop_detection.repeat_threshold`
  (default 3, clamped to ≥2), logging one JSONL line per loop and announcing each
  signature at most once. (2) **`--self-test`**: validates the config against
  `schemas/config_schema.json` with a stdlib JSON Schema subset, runs health
  checks, and derives a governance maturity level 1–5 written to
  `.ai/00-system/maturity-tracker.json`. (3) **`ANTHROPIC_API_KEY` warning** at
  `SessionStart` and in `--self-test`. Plus the `.ai/` governance library and the
  `templates/ai-library/` adopter scaffold, both tiered `live`/`convention`/
  `declarative`, and `docs/RETROSPECTIVE.md` activating the long-unused
  `retrospective_file` config key. 21 new tests, suite at 63.
- **Why:** These are the four v14 controls that are actually enforceable on Claude
  Code with a Pro subscription and no API key. Everything requiring a second agent,
  key infrastructure, or API access (DIDs, SPIFFE, delegation, shadow-AI detection,
  RAG metrics) is recorded as declarative with the reason attached, so no artifact
  implies enforcement it doesn't have.
- **Risk:** behavioral — loop detection is the first hook output that reacts to
  agent behaviour rather than file paths; kept advisory and announced once per
  signature so it can't become a nag.
- **Refs:** `hooks/workflow_hook.py` · `tests/test_hook.py` (63 OK) ·
  `schemas/config_schema.json` · `schemas/hook_contract.md` · `.ai/` ·
  `templates/ai-library/` · `docs/governance-integration-decision.md` ·
  `docs/RETROSPECTIVE.md` · `GUIDE.md` §12–§14 (v5.0)

### [decision] Tiered adoption instead of verbatim v14 adoption
- **What:** Adopted the full v14 artifact set but assigned every control one of
  three enforcement tiers, and recorded the disposition of all 24 sections in
  `docs/governance-integration-decision.md`. Kept Phase 0–3 canonical and mapped
  the 21-step SOP onto it (GUIDE §12) rather than renumbering. Dropped the RAG
  grounding/attribution/policy-alignment metrics outright rather than stubbing them.
- **Why:** v14 is written for an enterprise agent fleet with API access and key
  infrastructure. Instantiating it verbatim here would have produced artifacts that
  look enforced and are not — which is worse than not having them, because it
  teaches the reader to stop checking. Renumbering to 21 steps would have broken
  every archived plan, ledger entry and hook reference for no behavioural gain.
- **Risk:** design — a governance framework that overstates its own enforcement is
  a design flaw in the workflow itself.
- **Refs:** `docs/governance-integration-decision.md` · `GUIDE.md` §12–§14 ·
  `.ai/README.md`

### [workflow] Self-hardening loop closed on a real mistake
- **What:** Seeded `docs/RETROSPECTIVE.md` with two mistakes from this session.
  The heredoc-escape-mangling one is the *second* manifestation of a root cause
  already documented for `git commit -F -` (GUIDE v4.6), so it was marked
  `(recurring)` and codified into `.ai/01-phases/rule-no-heredoc-stdin.json`,
  GUIDE §4 Phase 2, and `SKILL.md`. That codification moved `--self-test` from
  maturity level 4 to 5.
- **Why:** Demonstrates the loop working on real data rather than a placeholder:
  mistake → second occurrence → rule → measurable level change.
- **Risk:** behavioral — heredoc corruption is silent until a pattern misses, and
  a partial match could have written a half-patched source file.
- **Refs:** `docs/RETROSPECTIVE.md` · `.ai/01-phases/rule-no-heredoc-stdin.json` ·
  `.ai/00-system/maturity-tracker.json`

## 2026-08-05

### [code] `tests/test_governance_library.py` — the checklist library becomes testable
- **What:** Added 33 tests over `.ai/`, covering what only prose covered before:
  manifests against the files on disk (both directions, so an unregistered rule is
  caught too), the required key set from `GROWTH.md`, `risk_source`/`risk_weight`
  against the CISA taxonomy, the GUIDE §12 step-mapping table against
  `01-phases/manifest.json`, and `.ai/` against its `templates/ai-library/` mirror.
  The load-bearing class is `EnforcementHonesty`: a `live` artifact must carry an
  `enforced_by` list of `path::symbol` references that resolve against the real
  source, a `convention` must carry an `enforcement_note`, and a `declarative` block
  must keep its values null. Suite: 63 → 96 tests.
- **Why:** The v14 integration landed a governance library whose central promise —
  no artifact may imply enforcement it doesn't have — was unfalsifiable, because
  nothing tied a tier claim to code. Prose cannot check itself.
- **Risk:** accountability — a governance library that cannot be audited is a claim,
  not a control, and the reader has no way to tell which it is.
- **Refs:** `tests/test_governance_library.py` · `.github/workflows/tests.yml` ·
  `docs/governance-integration-decision.md` §6

### [decision] Seven drifts found and corrected; two tiers were wrong
- **What:** Ran the new tests against the library as committed: 42 failures across
  9 test methods, one commit after the library was written. `rule-loop-detection`
  used a `risk_source` (`behavioral`) absent from the taxonomy it claimed to
  instantiate; six artifacts sat outside their weight band; `GROWTH.md` documented
  two invented sources and dropped two real ones; eight rules plus two blocks in
  `autonomy-boundaries.json` claimed `live` with nothing to point at;
  `rule-no-heredoc-stdin` claimed a v14 step whose GUIDE tier contradicted its own;
  the four prohibitions had no `provenance` despite `.ai/README.md` promising one;
  and `history/FORMAT.md` described the ledger's frozen slug list as the CISA
  categories, which it is not. Two tier claims were corrected: git sync **live →
  convention** (the hook reports a dirty tree, it does not sync), documentation
  standard **convention → live** (`--self-test` really does check frontmatter).
- **Why:** Every one of these was invisible to CI and to a reader skimming for a
  green check. The corrections matter less than the fact that the same class of
  error now fails a build.
- **Risk:** design — an enforcement tier that overstates itself trains the reader to
  stop verifying, which costs more than having no tier at all.
- **Refs:** `docs/governance-integration-decision.md` §6–§7 · `GUIDE.md` §12–§13 ·
  `.ai/GROWTH.md` · `history/FORMAT.md`

### [config] `--self-test` added to CI, gated on exit code only
- **What:** `.github/workflows/tests.yml` now runs
  `python hooks/workflow_hook.py --self-test` after the unittest step. Deliberately
  no `git diff --exit-code` on `maturity-tracker.json` and no new `--no-write` flag.
- **Why:** The tracker carries a `last_assessment` date stamp, so a cleanliness check
  would fail for the passage of time rather than for a regression — the exact mistake
  already recorded in `docs/RETROSPECTIVE.md`. Gating on tracker *contents* would also
  enforce the maturity level through the back door, when the level is reported and
  never enforced. The exit code carries the whole validation signal.
- **Refs:** `.github/workflows/tests.yml` · `hooks/workflow_hook.py` `run_self_test`

### [code] `PreToolUse` Tier-0 guard — three prohibitions become enforceable
- **What:** A fourth handler on the dispatcher (`handle_pre_tool_use`, matcher
  `Bash|PowerShell`) splits each command on the separators the permission matcher
  recognises and runs four guards over every subcommand. `guard_force_push` **denies**
  a force-push whose destination refspec resolves to a protected branch;
  `guard_protected_paths` **denies** `rm` / `Remove-Item` / `git rm` aimed at
  `tier0_guard.protected_paths`; `guard_history_rewrite` **asks** before `--amend`,
  `rebase`, `reset --hard`, `filter-branch`; `guard_heredoc` **asks** on heredocs,
  `-F -` / `--file=-` / `-F /dev/stdin`, and a bare `git commit`. New `tier0_guard`
  config block; 34 tests in `TestTier0Guard`.
- **Why:** `permissions.deny` is the stronger mechanism and does not fit these four.
  Patterns anchor on a literal prefix, so `Bash(git push --force*)` misses
  `git push origin main --force` and `git push -f`; a deny rule carries no exceptions,
  so one broad enough to protect `main` also blocks a topic branch you own; and
  `Bash(...)` / `PowerShell(...)` are separate namespaces, so a Bash-only list is
  bypassed by the other tool. Whether a branch is *shared* is a fact about the
  repository, not the command string — that needs a parser.
- **Risk:** capability — a guard that fires wrongly is worse than none, because it
  teaches the operator to route around it.
- **Refs:** `hooks/workflow_hook.py` · `schemas/hook_contract.md` §PreToolUse ·
  `GUIDE.md` §7.5 · `tests/test_hook.py::TestTier0Guard`

### [decision] `ask` is what made the guard buildable, and one prohibition stayed behind
- **What:** Four artifacts moved **convention → live**: the force-push, delete-ledger
  and rewrite-published-history prohibitions, plus `rule-no-heredoc-stdin`.
  `prohibition-commit-secrets` stayed `convention`. `rule-task-checklist` stayed
  `convention` with a note recording what `defaultMode: "plan"` does and does not buy.
- **Why:** `ROADMAP.md` had deferred this item over false-positive risk, and
  `rule-no-heredoc-stdin` v1.0 had declined a hook because "the false-positive cost on
  legitimate heredocs was judged higher than the failure it prevents". Both were
  weighing a *hard block*. Emitting `ask` where the command alone cannot settle the
  question inverts the trade: the call still cannot proceed without a human, but a
  false positive costs one keystroke instead of a wall. Secrets stayed behind because
  the guard sees the command, not the file contents being committed — it could only
  ever catch a secret typed inline, and shipping that as `live` would make three real
  controls and one piece of theatre indistinguishable.
- **Risk:** accountability — the whole value of the tier vocabulary is that `live`
  means something, so the one that could not earn it had to keep saying so.
- **Refs:** `docs/governance-integration-decision.md` §8 ·
  `.ai/02-market-rules/prohibitions/` · `ROADMAP.md`

### [config] Guard registered in settings; `defaultMode: "plan"`
- **What:** `.claude/settings.json` gains the `PreToolUse` entry on `Bash|PowerShell`
  and `permissions.defaultMode: "plan"`. `SettingsWiring` in
  `tests/test_governance_library.py` asserts both, and that nothing may cite
  `.claude/settings.json` as an enforcer without also citing the test that checks it.
- **Why:** `enforced_by` can prove `guard_force_push` exists; it cannot prove Claude
  Code ever calls it. That depends on a hook registration in a file no test read —
  delete the entry and the guard becomes unreachable code while every tier claim still
  resolves. That is the same invisible drift `tests/test_governance_library.py` was
  written to catch, one layer further out.
- **Refs:** `.claude/settings.json` · `tests/test_governance_library.py::SettingsWiring`

### [doc] GUIDE v5.1; revision history folded to `GUIDE_CHANGELOG.md`
- **What:** New GUIDE §7.5 (why a hook, not a deny rule; why two decisions), §7.1/§7.3
  gain the `PreToolUse` event and the fail-open consequence, §13 records the
  re-tierings. The Revision History moved to `GUIDE_CHANGELOG.md`
  (`exclude_from_ai: true`), latest three rows kept in place.
- **Why:** GUIDE's own v5.0 row instructed the fold "at the next edit" once the table
  reached 8 rows (§6.3). This was that edit. Named for its document rather than a bare
  `CHANGELOG.md`, which at the repository root would read as the project's changelog.
- **Refs:** `GUIDE.md` · `GUIDE_CHANGELOG.md` · `ROADMAP.md`

### [code] Stop flags gain a modification-time fallback
- **What:** `handle_stop` now treats `source_changed` / `ledger_touched` as a fast path and
  falls back to comparing modification times against `session_start_ts`. New
  `current_ledger_path` (extracted from `run_self_test`, so the `{year}-W{week:02d}.md`
  format has one definition) and `changed_since` (pruned `os.walk`, returns on the first
  file newer than the threshold, gives up after `mtime_scan_limit`). Config:
  `stop_hook.mtime_fallback` / `mtime_scan_limit` / `mtime_prune`.
- **Why:** `PostToolUse` fires only for `Edit|Write|MultiEdit`, so anything written another
  way is invisible to the flags. Both failure directions appeared in one session: a ledger
  appended by a shell redirect read as untouched, so the reminder nagged twice about an
  entry already committed and pushed; and a source file rewritten by `sed -i` would read as
  unchanged, so the reminder would never fire at all. A reminder that fires when the work
  was done trains you to dismiss it; one that never fires is worse.
- **Risk:** design — instrumentation that reports the opposite of the truth is worse than
  no instrumentation, because it is still believed.
- **Refs:** `hooks/workflow_hook.py` · `schemas/hook_contract.md` §Stop · `GUIDE.md` §7.3 ·
  `tests/test_hook.py::TestMtimeFallback`

### [decision] `FileChanged` evaluated and rejected; `live` split into deny and ask
- **What:** Two decisions. (1) Claude Code's `FileChanged` event was the obvious mechanism
  for the above — it watches the disk, so it sees writes from any process — and was
  rejected: its matcher is a list of literal filenames and this repo's ledger filename
  rolls over every Monday, so a static watch list would go stale in seven days. (2) Every
  guard-backed artifact now declares `enforcement_mode` (`deny` / `ask`), held to the code
  by `TestEnforcementModeMatchesGuard`.
- **Why:** (1) A watch list that silently stops matching is the "check that could never
  pass" already in `docs/RETROSPECTIVE.md`, and mtime is version-independent and
  indifferent to filenames. (2) `guard_heredoc` escalated a heredoc exactly as designed,
  the prompt was approved, and the rule was broken anyway — and that heredoc is what caused
  the ledger-flag failure above. An `ask` enforces the prompt, not the outcome, so it
  cannot wear the same tier as a `deny` without overstating itself. A prose note would have
  drifted; a field bound to the guard's actual return value cannot.
- **Risk:** accountability — the whole value of the tier vocabulary is that a reader can
  trust what `live` means without reading the implementation.
- **Refs:** `docs/governance-integration-decision.md` §9 · `docs/RETROSPECTIVE.md` ·
  `GUIDE.md` §13 · `.ai/README.md` · `ROADMAP.md`

### [code] Autonomous-run skills: `autonomous-task` and `handover`
- **What:** `skills/autonomous-task/SKILL.md` carries `disallowed-tools: AskUserQuestion`,
  so the option to interrupt is removed for the turn, and documents what to do instead:
  read the repository, then official documentation, then take the conservative option and
  record the assumption. `skills/handover/SKILL.md` writes `plans/HANDOVER.md` — the user's
  entire view of a run they did not watch.
- **Why:** The alternative was `permissions.defaultMode: "auto"` in `.claude/settings.json`,
  which does not work: Claude Code v2.1.142+ ignore `auto` from project settings so a
  repository cannot grant itself auto mode, and the proposed command used a `>` redirect
  that would have truncated the file and deleted the `PreToolUse` guard registration —
  silently voiding four `live` tier claims. `disallowed-tools` is documented for exactly
  this ("autonomous skills that should never call certain tools, such as
  `AskUserQuestion` for a background loop"), is local and session-scoped, and needs no
  third-party install.
- **Risk:** capability — removing the check-in raises the stakes on the handover, so the
  skill says plainly that autonomy is not licence to click through a guard's `ask`.
- **Refs:** `skills/autonomous-task/SKILL.md` · `skills/handover/SKILL.md` · `CLAUDE.md`

### [decision] Three `05-domains` checklists, written as questions
- **What:** `rule-solid-review`, `rule-clean-code-review`, `rule-security-review` — all
  `convention`, all phrased as review questions. `.ai/GROWTH.md` gains a fourth growth
  protocol (research) alongside breadth, depth and blueprint refinement.
  `06-components/BLUEPRINT_SCHEMA.md` defines the blueprint shape; the folder stays empty.
- **Why:** A prescription tells you what to type and stops being right the moment the
  codebase differs from the one it was written for; a question makes you look at the code
  and leaves judgement intact. Nothing mechanically decides whether a class has one
  responsibility, so the tier is `convention` and each file says what is missing. The
  blueprint folder stays empty on the same principle that seeded it empty: a pattern
  written down before it has shipped is a guess with a filename.
- **Risk:** design — a checklist that is too vague finds nothing and gets skipped, which
  costs more attention than having none.
- **Refs:** `.ai/05-domains/` · `.ai/06-components/BLUEPRINT_SCHEMA.md` · `.ai/GROWTH.md` ·
  `GUIDE.md` §13

### [decision] `artifacts()` widened so the new folder answers to the same invariants
- **What:** `tests/test_governance_library.py::artifacts()` now includes
  `.ai/05-domains/rule-*.json`, not just `01-phases/` and the prohibitions.
- **Why:** Without it the schema, taxonomy, provenance and enforcement-honesty checks
  would simply not have applied to the new folder — a place where half the invariants
  quietly did not hold, which is how the drift these tests exist to catch gets in.
  Confirmed by breaking a domain rule six ways (bad `risk_source`, out-of-band weight, a
  `live` claim with no enforcer, missing `provenance`, an unregistered file, an unmirrored
  edit); each was caught by the check that should catch it.
- **Risk:** structural — an exemption nobody noticed is worse than one written down.
- **Refs:** `tests/test_governance_library.py`

### [doc] SOLID checklist trialled against the hook; findings recorded not fixed
- **What:** Ran the new checklist over `hooks/workflow_hook.py` (1,463 lines, 50 functions,
  1 class). It found `run_self_test` at ~166 lines and `handle_stop` at ~89 both failing
  the "describe it without saying *and*" question, and 16
  `except Exception: pass/return/continue` sites. It also found that four of the five SOLID
  principles barely apply to a procedural module — recorded, because a checklist claiming
  to apply everywhere is suspect.
- **Why:** A checklist that finds nothing on real code is too vague to be worth loading,
  and reading it would not have revealed that. Acting on the findings is a refactor and a
  separate task; the audit's job was to establish the checklist has teeth.
- **Refs:** `ROADMAP.md` · `.ai/05-domains/rule-solid-review.json`

### [code] The Stop hook stops counting its own breadcrumb as work
- **What:** `dirty_excluding_breadcrumb` and `has_outstanding_plan` ignore
  `plans/UNFINISHED.md` when it carries `BREADCRUMB_MARKER`; a human-authored plan at the
  same path still counts. `git status --porcelain -uall` so a wholly untracked `plans/`
  does not collapse to one entry and hide the file inside it. The dirty check only ever
  *clears* a false positive - `git_status` stays the authority on whether work exists.
- **Why:** The hook wrote the breadcrumb on a dirty tree, that file was itself untracked,
  and the next Stop counted it as more unfinished work - a loop that manufactured the
  state it complained about and could not clear on its own. `--self-test` then reported an
  outstanding plan on top of it.
- **Risk:** design - a reminder that fires on a state the user considers normal gets
  dismissed, and once dismissed it is dismissed for the real cases too.
- **Refs:** `hooks/workflow_hook.py` · `tests/test_hook.py::TestBreadcrumbNotCountedAsWork`
  · `docs/RETROSPECTIVE.md`

### [code] `--self-test` reports which gate blocked the maturity climb
- **What:** The ladder still stops at the first failed gate, but the report now names that
  gate and lists the checks already passing above it. Also new: `documentation_exclude`
  config key and `exclude_from_ai: true` frontmatter support, resolved through one
  `is_excluded_doc` predicate; and a revalidation warning when a checklist item's
  `last_validated` exceeds `revalidation_interval_days` (default 180).
- **Why:** It read "level 2/5 (Repeatable)" while seven of nine checks passed - one warn at
  level 3 hid every passing check at levels 4 and 5. A number that says "immature" when one
  box is unticked teaches the reader to ignore it. The doc exclusion closes the
  `exclude_from_ai` backlog item, which nothing had implemented.
- **Risk:** accountability - the maturity level is the one number a reader takes at face
  value.
- **Refs:** `hooks/workflow_hook.py` · `schemas/config_schema.json` · `GUIDE.md` §7.4

### [decision] Both staging documents adopted; ten sourced checklist categories
- **What:** `docs/to-be-implement/` is gone. Its two documents are consolidated into
  `docs/checklist-system.md` and instantiated as ten categories in `.ai/05-domains/` - 80
  items, each citing a document with an authority tier, a `source_version`, and a
  `confidence_level` **derived** by `derive_confidence` and recomputed by test. Conditional
  loading is manifest data read by both skills. SOLID and clean-code folded into the
  architecture and code-quality categories; security re-sourced against ASVS 5.0.0.
- **Why:** Validating the documents before adopting them found five citation defects,
  including "OWASP ASVS 5.10" - two levels of a three-level scheme, where the real
  requirement is V1.2.4. A protocol that finds five defects in its own companion documents
  is the strongest argument for adopting it. Dropped: confidence percentages (no procedure
  separates 94% from 96%) and the +/-2 self-adjustment (the agent that applied a rule would
  also grade its own false-positive rate, with no ground truth in the loop).
- **Risk:** accountability - a checklist that asserts an unearned confidence is the same
  failure as a faked enforcement tier.
- **Refs:** `docs/checklist-system.md` · `.ai/05-domains/` · `.ai/GROWTH.md` ·
  `tests/test_governance_library.py::ChecklistItems` · `GUIDE.md` §13

### [doc] The `FileChanged` probe recorded as inconclusive
- **What:** A temporary matcher-less `FileChanged` entry pointing at a throwaway script
  logged nothing when a file was written by a shell process. Recorded in
  `docs/governance-integration-decision.md` §10 as **inconclusive**, not negative.
- **Why:** Three confounders survive and none can be eliminated from inside one session:
  hook registrations may need a restart, the event needs v2.1.146+ and this environment
  cannot report its own CLI version, and an empty matcher may mean "watch nothing" for an
  event whose matcher is a watch list. Reporting a negative would be the same overreach as
  the NIST citation this record corrected. The probe used a scratch script rather than a
  `--probe-filechanged` flag: permanent surface for a one-off question is the objection
  already raised against `--no-write` and `--verify-sources`.
- **Risk:** design - an inconclusive result recorded as a finding is worse than no result.
- **Refs:** `docs/governance-integration-decision.md` §10 · `ROADMAP.md`

### [decision] Plan review: the phase the checklists could not reach
- **What:** New `.ai/03-planning/` - five categories, 31 items, answered at Phase 1 against
  the plan itself: problem definition, requirements, risk and reversibility, alternatives
  and rationale, verification planning. Every item cites a current edition (ISO/IEC/IEEE
  29148:2018, 42010:2022, IEEE 1012-2024, ISO 31000:2018, INCOSE SEH 5th ed., NASA/SP-2016-
  6105 Rev 2, Google SRE, Threat Modeling Manifesto, Nygard's ADR) with a
  `confidence_level` derived by `derive_confidence`. Never gated on tech stack.
- **Why:** Every rule in `05-domains/` says the same thing in its `ai_action` - "work
  through the checklist against the code in question" - and `rule-task-checklist` told the
  agent to produce a plan checklist while supplying nothing to interrogate it with. So a
  plan was reviewed by nothing, and not by oversight: code review measures code *against*
  the plan, so it will confirm that a well-built thing was built well when the thing should
  not have been built at all. No amount of Phase-2 review reaches a defect one level up.
- **Risk:** design - a plan is the cheapest artifact to fix and the most expensive to get
  wrong, because everything downstream inherits its mistakes.
- **Refs:** `.ai/03-planning/` · `docs/checklist-system.md` §5a · `GUIDE.md` §13 ·
  `.ai/GROWTH.md` · `.ai/01-phases/rule-task-checklist.json`

### [decision] One selection table, moved out of the folder it outgrew
- **What:** The `selection` block moves from `.ai/05-domains/manifest.json` to
  `.ai/00-system/checklist-selection.json` and gains a `phases` axis; every checklist rule
  gains `workflow_phase`. Both skills read the new path.
  `ChecklistSelection.test_no_folder_manifest_defines_its_own_selection` fails the build if
  a folder grows its own table again.
- **Why:** Plan rules needed selecting too. Leaving the table where it was and adding a
  second to `03-planning/manifest.json` would have made two definitions of one thing - the
  drift this library exists to prevent, and the objection already recorded against
  restating loading logic inside each skill. A domain manifest cannot be the authority on a
  folder it does not contain.
- **Risk:** structural - two tables answer differently and both look right.
- **Note:** This is a **breaking path change for adopters** who vendored the library before
  today and read `selection` from the domain manifest. Recorded in `docs/checklist-system.md`
  §5 rather than left to be discovered. Found by `RSK-REV-02` during the trial below, not
  by design.
- **Refs:** `.ai/00-system/checklist-selection.json` (+ `.prov.md`) ·
  `tests/test_governance_library.py::ChecklistSelection` · `skills/*/SKILL.md`

### [test] The plan checklists run against their own plan; 14/14 negative control
- **What:** `.ai/GROWTH.md` requires a new checklist be run against real work before it is
  kept. These were run against the plan that produced them: **10 of 31 items found
  something**, including no out-of-scope list, no stated rollback for the selection-table
  move, "do nothing" never considered, and the unnamed load-bearing assumption - that a
  plan reviewed by the same agent that wrote it is worth anything. Separately, 14 deliberate
  breakages were each caught by the specific check that should catch it.
- **Why:** A checklist that flatters the plan it shipped with has not been tested. The
  negative control needed two corrections before it proved anything: the first run had an
  already-red baseline, so every breakage reported FAILED regardless; the second kept its
  backup inside `.ai/`, so `TemplateParity` fired on every breakage whether or not the
  intended check did. Both are recorded in `RETROSPECTIVE.md` - a control that reports a
  count answers "is anything red", which is a different question that looks identical.
- **Risk:** accountability - evidence that appears to say a thing was verified, when it was
  not, is worse than no evidence.
- **Refs:** `tests/test_governance_library.py` (189 tests) · `docs/checklist-system.md` §5a
  · `docs/RETROSPECTIVE.md`

### [doc] The Claude Code version was one directory listing away, for two sessions
- **What:** This environment runs **Claude Code 2.1.221**, read from
  `~/.vscode/extensions/anthropic.claude-code-2.1.221-win32-x64`. The `FileChanged` probe's
  confounder list drops from three to two, and the `opus`-alias caveat (needs v2.1.219+)
  resolves here. `--self-test` now prints where to find the version when `claude` is not on
  PATH.
- **Why:** "This environment cannot report its own version" was recorded in the decision
  record, carried into the handover, and asked of the user twice. It was true about the
  method tried - the CLI genuinely is not on PATH under the VS Code extension - which is
  exactly what let it survive as though it were true about the question. Logged as
  **recurring** against the 2026-08-04 entry about a health check that could never pass:
  both are claims about the environment adopted without a probe.
- **Risk:** accountability - a standing caveat keeps a finding inconclusive and an item on
  the user's plate, so it should cost at least one attempt before it is written down.
- **Refs:** `docs/governance-integration-decision.md` §10 · `docs/RETROSPECTIVE.md` ·
  `hooks/workflow_hook.py` · `ROADMAP.md`

## 2026-08-06

### [workflow] The skills move to where Claude Code actually looks for them
- **What:** `git mv skills .claude/skills`. Claude Code discovers skills at
  `~/.claude/skills/`, the project `.claude/skills/` (plus nested ones below the working
  directory) and plugin directories, and **nowhere else** — there is no settings key or
  manifest that registers an alternative root. So the three skills at the repo root were
  never loaded as skills: no `/adaptive-workflow` command existed, no description ever
  triggered an automatic load, and `disallowed-tools: AskUserQuestion` on `autonomous-task`
  was inert for its entire life. They worked only because `CLAUDE.md` named their paths in
  prose. Confirmed fixed in-session: all four now appear in the skill listing. Path
  references updated in `CLAUDE.md`, `GUIDE.md` §2.1/§2.2, `README.md`,
  `docs/checklist-system.md`, `docs/claude-code-hook-integration/index.md`,
  `.claude/workflow_config.json` (`source_directories`), and both mirrored `.ai/` +
  `templates/ai-library/` pairs. `history/` and the changelogs are left alone — they record
  what was true when written.
- **Why:** This is the failure mode the repository exists to prevent, in the repository
  itself: a control that reads as binding, cannot bind, and reports nothing either way.
  `CLAUDE.md` forbids implying enforcement that does not exist; a frontmatter field in an
  undiscovered directory is exactly that. Nothing checked the one property no artifact test
  covers — whether the runtime can see the file at all.
- **Risk:** structural - the mechanism delivering the governance was disconnected from the
  governance, and every check passed anyway.
- **Refs:** `.claude/skills/` · `docs/RETROSPECTIVE.md` (2026-08-06) ·
  `tests/test_skills.py` · <https://code.claude.com/docs/en/skills>

### [workflow] `skill-authoring`, and a test that keeps skills small and discoverable
- **What:** New `tests/test_skills.py` (25 tests, stdlib-only, flat frontmatter parser rather
  than a PyYAML dependency). Checks entrypoint presence and **casing** — read from the
  directory entries, because a case-insensitive filesystem answers `is_file()` for a
  `skill.md` Claude Code would never load — kebab-case names, `name`/directory agreement,
  description length, the documented 500-line body cap, unknown frontmatter keys, markdown
  link resolution, and `templates/skills/` parity. It also **warns** past a ~5,000-token
  proxy measured in characters, not lines. 15 of the 25 are negative controls that assert
  the *named* violation each defect must produce. New `skill-authoring` skill carries the
  authoring rules; `adaptive-workflow` split 260 → 183 lines with three `references/`.
  `.ai/05-domains/rule-skill-authoring-review.json` (8 items, `convention`) harvested from
  the defect above, mirrored into `templates/ai-library/`.
- **Why:** Two limits, and the tighter one is undocumented as a limit: after auto-compaction
  Claude Code re-attaches only the **first 5,000 tokens** of each skill, with all re-attached
  skills sharing a 25,000-token budget. A skill past that is truncated silently, exactly when
  context is scarcest. Line count measures wrapping, not content — this repo's skill markdown
  runs ~59 chars/line, so a line-based proxy misjudges a dense file by more than 2×. The
  rule is `convention`, not `live`, because the test cannot judge whether a skill has one
  responsibility; the `enforcement_note` says which half is mechanical.
- **Risk:** design - guidance that exists but never reaches the agent leaves the work
  proceeding without it, and nothing reports the absence.
- **Refs:** `tests/test_skills.py` · `.claude/skills/skill-authoring/` ·
  `.claude/skills/skill-authoring/references/verified-facts.md` ·
  `.ai/05-domains/rule-skill-authoring-review.json` · `templates/skills/` ·
  `templates/CLAUDE.md.fragment`

### [config] Every adopter was shipped a settings template with no Tier-0 guard
- **What:** `templates/settings.json.hooks` — the fragment adopters merge into
  `.claude/settings.json` — registered `SessionStart`, `PostToolUse` and `Stop`, and **no
  `PreToolUse` block**, plus no `permissions.defaultMode`. `PreToolUse` is the only event
  the guard runs on, so in every adopting project a force-push to a protected branch was
  not denied, deleting `history/` or `plans/archive/` was not denied, and history rewrites
  and heredocs never asked. Measured with a harness that reads the settings, resolves the
  matcher against the tool name, then runs the dispatcher — the order Claude Code uses:
  **0 of 3** Tier-0 probes denied under the template, 3 of 3 under this repo's own
  settings. After the fix, 3 of 3 under both.
- **Why:** `SettingsWiring` verified the wiring in `.claude/settings.json` only — its
  docstring called that "a file no other test reads", which was true and was exactly the
  hole. `EnforcementHonesty` passed for the wrong reason: `enforced_by` resolved to real
  `guard_*` symbols, and the config that would invoke them was absent from what ships. Four
  artifacts claimed `live` on code no adopter could reach. `SettingsWiring` now checks both
  configs with a per-config expected dispatcher path; reverting the template fails all five
  assertions, each naming `config='adopter template'`.
- **Risk:** privilege - a security-adjacent control documented as enforced, absent in the
  distributed artifact, for everyone except the repository that wrote it.
- **Refs:** `templates/settings.json.hooks` · `tests/test_governance_library.py::SettingsWiring`
  · `CHANGELOG.md` (merge instructions for existing installs) · commit `84db234`

### [config] `settings.local.json` was ignored by one machine, not by the repository
- **What:** `.claude/settings.local.json` was untracked here solely because of
  `~/.config/git/ignore`, a user-global rule no clone inherits. The repo's own `.gitignore`
  said nothing, so on any other checkout `git add -A` would sweep personal permission grants
  into the tree. Added the rule, and verified with `core.excludesFile=/dev/null` that it now
  holds without the global file. Adoption steps in `GUIDE.md` §8.1 and `README.md` also
  gained the three things nothing previously told an adopter: merge `defaultMode` as well as
  the hooks block *and verify a force-push is denied*, copy `templates/skills/` into
  `.claude/skills/` because only that path is discovered, and gitignore this file.
- **Why:** Claude Code's own directory docs badge the file gitignored. This repo already has
  a commit for the same class of accident (`3feacb3`).
- **Risk:** privilege - the file holds permission grants, and the protection was per-machine.
- **Refs:** `.gitignore` · `GUIDE.md` §8.1 · `README.md` · commit `212735b`

### [doc] A reference page is not the whole record
- **What:** Two wrong statements shipped last session, both from treating the skills docs
  page as complete. `verified-facts.md` recorded "no documented minimum" for
  `disallowed-tools`; the floor is **v2.1.152 (27 May 2026)**, in the release notes. The
  same release added **`/reload-skills`**, which supersedes the "restart Claude Code" advice
  I had put in four places, including the adopter fragment. Corrected everywhere, restart
  kept only as a fallback. `verified-facts.md` now opens with a table naming which of the
  three sources — docs page, hooks reference, release notes — carries which kind of fact,
  and a version-floor table sourced from the release notes.
- **Why:** the reasoning was sound and the conclusion was wrong: the docs page annotates
  twelve version floors and gives none for that field, so an absence looked like evidence.
  It was evidence about one page. Also folded in the v4.0 guide deltas — `background` is
  fork-only, `shell` is real, `version` is not a field at all.
- **Risk:** accountability - a confidently recorded absence is harder to dislodge than an
  open question, and this one was carried into a shipped artifact.
- **Refs:** `.claude/skills/skill-authoring/references/verified-facts.md` ·
  `docs/RETROSPECTIVE.md` · commit `cec82ba`

### [code] A guard for where Claude Code configuration has to live
- **What:** New `claude-code-layout` skill (109 lines + two references) and
  `tests/test_claude_layout.py` (19 tests, 12 of them negative controls). The skill carries
  the distinction the whole failure class turns on — **discovered by location** (`skills/`,
  `agents/`, `commands/`, `output-styles/`, `rules/`, `workflows/`) versus **registered
  elsewhere** (hooks) — the verified tree, and the two precedence orders that disagree about
  user-vs-project. The test checks no discovered directory sits outside `.claude/`, every
  registered hook names a real event and an existing script, both settings files register the
  same events, agent files carry `name`/`description` with no `:` and no tree-wide
  duplicates, unknown files under `.claude/` are on an allowlist, and `settings.local.json`
  is ignored by the repo's own rules.
- **Why:** three defects in three sessions shared one shape — correct content that nothing
  consumes, no error, suite green throughout. Two false positives in the first draft were
  caught by the negative controls and fixed: `.github/workflows` is GitHub Actions, and the
  adopter template's script path resolves in an adopting project rather than here.
- **Risk:** structural - the mechanism delivering the governance kept being disconnected from
  the governance, and nothing looked at the join.
- **Refs:** `.claude/skills/claude-code-layout/` · `tests/test_claude_layout.py` ·
  commit `6833df7`

### [code] Skills reload at session start, and edits to one get a nudge
- **What:** `SessionStart` emits `hookSpecificOutput.reloadSkills: true` when
  `.claude/skills/` exists — live change detection does not watch a top-level skills
  directory created after session start, which is the mechanical form of the original
  defect. `PostToolUse` gains a one-time advisory (`skill_nudged`) on edits under
  `.claude/skills/`. `schemas/hook_contract.md`, `GUIDE.md` §7.2/§7.3 and the
  `adaptive-workflow` trigger table updated together.
- **Why:** the advisory **fired live on the commit that introduced it**, which is better
  evidence than the synthetic event. All three new hook tests were run against the
  unmodified dispatcher first and failed there.
- **Risk:** design - both are advisory by construction; `PostToolUse` fires after the write,
  so it cannot block, and the contract says so rather than implying a gate.
- **Refs:** `hooks/workflow_hook.py` · `schemas/hook_contract.md` · `GUIDE.md` §7 ·
  `tests/test_hook.py` · commit `ad29273`

### [decision] Disputes about system behaviour get settled by direct test
- **What:** New `convention` rule `.ai/01-phases/rule-direct-test-disputes.json` (+ manifest
  → v1.3, 13 items, + `templates/ai-library/` mirror) and a fourth execution guard in the
  `adaptive-workflow` skill. Two parts: run the test instead of citing documentation, testing
  *every* competing claim rather than the one you expect to win; and when the user has to run
  it, write the instructions for someone with no expertise — what to do, what a pass looks
  like, what a failure looks like, why you cannot do it yourself.
- **Why:** planning this task, a vendored guide and this repo's own skill disagreed about
  whether the `Stop` hook can block. Two rounds of citation settled nothing. Executing the
  hook settled it in one command. Recorded as `accountability` risk because the failure mode
  is an unverified claim entering the record, not a broken build.
- **Risk:** accountability - and the rule is honest that **nothing enforces it**: no hook can
  distinguish a tested claim from a confident one, since both arrive as ordinary assistant
  text.
- **Refs:** `.ai/01-phases/rule-direct-test-disputes.json` ·
  `.claude/skills/adaptive-workflow/SKILL.md` · `templates/ai-library/01-phases/`

### [doc] The `Stop` hook blocks, observed end-to-end
- **What:** Settled the disputed claim from the layout guide v6.0, which lists `Stop` as
  non-blocking. Three independent levels agree it is wrong: the live docs' "Exit code 2
  behavior per event" table says *"Yes | Prevents Claude from stopping, continues the
  conversation"*; `handle_stop` emits `{"decision": "block", "reason": ...}`, confirmed by
  executing the dispatcher directly; and **the block fired in a live turn** — the session did
  not end, the reason surfaced, and the turn resumed. Same pass also confirmed
  `guard_protected_paths` and `guard_force_push` return `permissionDecision: "deny"` when
  executed against `rm -rf history/` and `git push --force origin main`.
- **Why:** first application of the rule above. The end-to-end observation is what the
  document-level argument could not supply: emitting a block and the platform *honouring* it
  are two different claims, and only the second was in doubt.
- **Risk:** accountability - the guide is designated this project's golden rule, so an error
  in it propagates into every artifact derived from it.
- **Refs:** `hooks/workflow_hook.py::handle_stop` · `tests/test_hook.py::TestStop` ·
  <https://code.claude.com/docs/en/hooks>

### [config] Two corrections to the approved plan, found by reading the tests
- **What:** (1) The plan required a `GUIDE.md` §12 row for the new rule; `StepMapping` does
  `if step is None: continue`, so a rule with `v14_step: null` needs none. (2) The plan
  assumed phase rules carry a `checklist` array with per-item sourcing; they do not — that
  applies to `03-planning`/`05-domains`. `EnforcementHonesty` additionally requires a
  `convention` rule to name **no** `enforced_by` at all.
- **Why:** logged because both were confident assumptions in an approved plan that the code
  contradicted — the same failure the new rule exists to prevent, one stage after writing it.
- **Risk:** design - no artifact shipped wrong; the cost was avoided rework.
- **Refs:** `tests/test_governance_library.py::StepMapping` · `::EnforcementHonesty`

### [decision] A heredoc ran despite the guard returning `ask` — cause undetermined
- **What:** While writing the entries above, a `cat >> history/2026-W32.md << 'LEDGER_EOF'`
  was run in violation of the standing no-heredoc rule. It executed with no prompt and
  appended a stray `PLACEHOLDER` line, reverted immediately with the editing tools. Replaying
  **the exact same command** through the dispatcher afterwards returns
  `permissionDecision: "ask"` with the `rule-no-heredoc-stdin` reason. So `guard_heredoc` is
  correct and was not bypassed by the command's shape.
- **Why:** logged because the two halves fail differently and only one is fixed. The agent
  error is mine — the rule is unambiguous and I ran the heredoc anyway. The environment half
  is **unexplained**: an `ask` decision did not surface as a prompt in this session, and I
  cannot determine from inside the session whether that is permission mode, a `dontAsk`
  setting, or something else. Not guessing. GUIDE §7.5 already warns that `dontAsk` converts
  an `ask` into a silent block; what was observed here is different — a silent *allow*.
- **Risk:** privilege - a Tier-1 `ask` that neither prompts nor blocks is, in that session,
  no control at all, and nothing in the repo would have revealed it.
- **Refs:** `hooks/workflow_hook.py::guard_heredoc` · `GUIDE.md` §7.5 ·
  `.ai/01-phases/rule-no-heredoc-stdin.json` · ROADMAP (follow-up to determine the cause)

### [workflow] `git add -A` swept in the auto-breadcrumb again (recurring)
- **What:** `plans/UNFINISHED.md` was committed in `6f1175a` by a blanket `git add -A`,
  untracked again with `git rm --cached`. Codified rather than just fixed: the
  `adaptive-workflow` Phase 3 commit step now carries the check inline — the Stop hook
  rewrites that breadcrumb on every dirty-tree turn, so it is almost always present when
  committing mid-task, and `head -1 plans/UNFINISHED.md` showing the
  `<!-- workflow-hook: auto-breadcrumb -->` marker means do not commit it.
- **Why:** **second occurrence.** `ceb17c2` committed it, `3feacb3` removed it with the
  reasoning that the path must stay un-gitignored because a human-authored plan lives there
  and ignoring it would make that plan uncommittable. That reasoning still holds, so the
  recurrence could not be fixed by ignoring the path — the skill instruction that produced it
  (`git add -A && git commit`, stated with no caveat) was the actual cause and is what changed.
- **Risk:** structural - transient session state entering the permanent record, from an
  instruction the skill itself gives.
- **Refs:** `.claude/skills/adaptive-workflow/SKILL.md` Phase 3 · `templates/skills/` mirror ·
  commits `ceb17c2`, `3feacb3`, `6f1175a`

### [config] Dispatcher moved to `.claude/hooks/workflow_hook.py`
- **What:** `git mv hooks/ .claude/hooks/`, with 53 files rewritten across two path
  conventions — the repo's own (`.claude/hooks/…`) and the adopter's, which gains a doubled
  segment (`.claude/workflow-core/.claude/hooks/…`) because the submodule now carries its own
  `.claude/`. Also updated: both `sys.path` inserts in the test suite, `SettingsWiring.CONFIGS`,
  `source_directories`, CI, and a **BREAKING** CHANGELOG entry with the literal
  find/replace string and a guard probe adopters can run.
- **Why:** requested, to match the layout guide's "hook scripts live in `.claude/hooks/`"
  convention. Recorded plainly: this was **not** a defect fix. Hook scripts are *registered*
  in `settings.json`, never discovered by location, so the old path worked; the guide itself
  says so. `history/` was deliberately not rewritten — ledger entries record what was true
  when written.
- **Risk:** structural - the move is invisible to tests in an adopter's repo, where their own
  `settings.json` still names the old path and the dispatcher fails open.
- **Refs:** `.claude/hooks/workflow_hook.py` · `.claude/settings.json` ·
  `templates/settings.json.hooks` · `CHANGELOG.md` · `tests/test_claude_layout.py`

### [code] `--self-test` was reporting PASS while validating nothing
- **What:** The move broke schema resolution: `__file__.parent.parent / "schemas"` assumed the
  dispatcher sat one level below the repo root, and resolved to `.claude/schemas/`. Because an
  unreachable schema only *warns*, `--self-test` kept exiting 0 and printing `RESULT: PASS`
  with two knowingly-invalid configs passing. Replaced with `find_schema_path()`, an upward
  walk mirroring `find_config_path()`, which holds for both layouts.
- **Why:** **the approved plan asserted "no change to the script itself", and that was wrong.**
  `find_config_path()` was verified to walk upward and the conclusion was generalised to the
  whole file without checking the second path expression. Caught by the existing suite, not by
  review. New `TestSchemaLocation` asserts on the positive `config valid against …` line rather
  than the exit code, because exit 0 cannot distinguish "validated and passed" from "skipped
  and defaulted to passing" — which is precisely how it hid. The negative control was executed,
  not assumed: with the schema unreachable, `--self-test` prints PASS at exit 0 and the new
  test fails.
- **Risk:** accountability - a validator that silently stops validating while still reporting
  success is worse than no validator.
- **Refs:** `.claude/hooks/workflow_hook.py::find_schema_path` ·
  `tests/test_hook.py::TestSchemaLocation` · `CHANGELOG.md`

### [code] Directories under `.claude/` were never checked
- **What:** `NonStandardFiles` filtered `iterdir()` on `is_file()`, so any *directory* placed
  under `.claude/` went unexamined — including the new `hooks/`. Added
  `test_unknown_directories_are_on_the_allowlist` with a `STANDARD_CLAUDE_DIRS` allowlist, plus
  negative controls asserting that near-miss names (`skill`, `rule`, `agent`, `hook`) are *not*
  allowlisted and that `hooks` is not in `DISCOVERED_DIRS`.
- **Why:** a directory is the worse of the two mistakes — a stray file is inert, but
  `.claude/skill/` or `.claude/rule/` *looks* discovered, is read by nothing, and reports
  nothing. Found while adding `.claude/hooks/` and noticing the check would not have objected
  to any name at all.
- **Risk:** structural - same silent-inertness class as the three defects that motivated
  `tests/test_claude_layout.py` in the first place.
- **Refs:** `tests/test_claude_layout.py::NonStandardFiles` · `::NegativeControls`

### [doc] Layout guide vendored as the golden rule, with its errors recorded
- **What:** `docs/claude-code-layout-guide/index.md` (v6.1, `official: false`). Carries the
  received guide's provenance — its own date is 2026-08-06 but it cites docs dated 2026-07-16
  → 2026-07-29 — plus a Corrections section for three verified errors: the hook-event blocking
  table, a 24-event list where the live docs have 31, and "exit 2 blocks (PreToolUse only)".
  Precedence stated explicitly: live docs beat the guide, the running system beats both.
- **Why:** the user designated it the project's golden rule. Adopting it *verbatim* would have
  regressed the repo, whose `HOOK_EVENTS` list is already correct — and its `Stop`-cannot-block
  claim is contradicted by this repo's own hook, so encoding it would have failed the suite
  against reality. Recording the errata rather than silently patching the body keeps a reader
  able to diff this copy against the original.
- **Risk:** accountability - a golden-rule document that is wrong about its own subject
  propagates into everything derived from it.
- **Refs:** `docs/claude-code-layout-guide/index.md` ·
  `.claude/skills/claude-code-layout/SKILL.md`

### [doc] Re-verified against v2.1.223; `effort`/`maxTurns` were wrongly marked unverified
- **What:** Re-stamped four files from v2.1.221 to **v2.1.223** after re-reading the skills,
  sub-agents, hooks and memory doc pages. Substantive corrections: `frontmatter-by-type.md`
  listed subagent `effort` and `maxTurns` under "Not verified — appear in third-party guides
  but not the documented field table"; **both are in the table**. Added the eight subagent
  fields the table omitted (`maxTurns`, `mcpServers`, `hooks`, `memory`, `background`,
  `effort`, `isolation`, `initialPrompt`), three inert skill fields (`metadata`, `license`,
  `compatibility`), and three version floors (`name` with `:` unloaded as of v2.1.218;
  booleans accepting yes/no/on/off from v2.1.218; background-by-default from v2.1.198).
- **Why:** the stamps read v2.1.221 while **v2.1.223** was installed — stale within days, with
  nothing observing it. The `effort`/`maxTurns` entry is the second instance of recording an
  absence from an incomplete read of a page; `verified-facts.md` already documents the first.
  Also fixed the procedure that caused the wrong version: read the **highest** installed
  extension, not the first — two coexist here.
- **Risk:** design - a confident "not documented" is more harmful than an admitted gap.
- **Refs:** `.claude/skills/claude-code-layout/references/frontmatter-by-type.md` ·
  `references/directory-map.md` · `skill-authoring/references/*.md` ·
  `tests/test_claude_layout.py`

### [code] `layout-auditor` subagent — `.claude/agents/` has its first occupant
- **What:** A read-only auditor (`disallowedTools: Write, Edit, NotebookEdit`) that checks
  discovered-vs-registered wiring across **both** settings files, near-miss directory names,
  subagent identity rules, and whether `.ai/` `live` claims resolve to code that actually runs.
  Instructed to prefer executed evidence over inference, and to close with what it could not
  verify.
- **Why:** `.claude/agents/` was empty, so `agent_problems()` in `tests/test_claude_layout.py`
  had never run against a real file — the checker itself was unexercised. It now validates a
  real agent. The auditor's brief encodes the adopter-template asymmetry specifically, because
  that is the defect that shipped unnoticed.
- **Risk:** design - advisory by construction; it reports and cannot edit.
- **Refs:** `.claude/agents/layout-auditor.md` · `tests/test_claude_layout.py::SubagentFiles`
