# 2026-W29  (2026-07-13 – 2026-07-19)

## 2026-07-18

### [decision] Evaluated the ODB (`odb_library`) workflow review — accepted 1, rejected the rest
- **What:** A sibling repo that cherry-picked features **from** this repo
  (config-driven hook, `plans/UNFINISHED.md` breadcrumb, doc provenance) sent back a
  review listing its own additions and rejections. Treated it as inbound feedback and
  ruled per item:
  - **ACCEPT** — `last_validated` should record the last *content* review, not the
    last edit (ODB's "honesty note"). This matched a genuine self-contradiction in our
    Documentation Standard. Implemented (see the `[doc]` entry below).
  - **REJECT** — weekly `history/` ledger (their "redundant/rot-prone" critique):
    valid for ODB, which has RETROSPECTIVE + Conventional Commits + per-package
    CHANGELOGs as substitutes; **this repo has none of those**, so the ledger is the
    primary, Stop-hook-enforced self-correction record. Rot mitigation (roadmap
    "ledger summarizer") is planned, not yet built — rejection rests on the
    substitute-gap.
  - **REJECT** — `estimated_tokens` linting as "over-engineering": the field feeds our
    Progressive Disclosure retrieval script (foundational here, absent in ODB); the
    linter itself is unbuilt backlog.
  - **REJECT** — backporting ODB's 10-gate Definition of Done and 13 skills / 6 agents:
    Dart/Flutter-specific verticalization that breaks workflow-core's portability
    (intentionally one skill + one stdlib hook, vendorable dependency-free).
  - **REJECT (as mandatory)** — ODB's always-on RETROSPECTIVE loop: we already have
    opt-in retro scaffolding, currently half-wired (`retrospective_file` referenced in
    `schemas/config_schema.json`, `templates/workflow_config.json`, `GUIDE.md`,
    `SKILL.md`, but no `docs/RETROSPECTIVE.md` exists and this repo doesn't set it).
    Wiring/removing those dangling refs deferred as a separate cleanup.
  - **Already aligned** — ODB's rejection of auto git-sync at SessionStart: this repo
    never auto-syncs; SessionStart only *reports* ahead/behind.
- **Why:** ODB is a deep vertical fork; this repo is the portable source. The review's
  ODB-exclusive features are domain specializations, and its rejections mostly apply to
  ODB's substitute mechanisms that this repo lacks — so cherry-picking, not adopting
  wholesale, was correct. Recording the reject rationales here is the deliverable.
- **Refs:** ODB review (pasted in session — no separate document reachable from this
  repo) · `ROADMAP.md` · `.claude/workflow_config.json` · plan
  `get-the-latest-changes-sleepy-hellman`

### [doc] Documentation Standard — `last_validated` = last content review, not last edit
- **What:** Resolved a contradiction: `DOC_TEMPLATE.md` defined `last_validated` as
  "date you last confirmed the content is correct" while `GUIDE.md` §6.3/§6.5, the
  embedded template comment, and the `SKILL.md` mirror all said "refresh on every
  non-trivial edit." Reworded so a mechanical/frontmatter-only edit bumps `version` +
  adds a Revision History row but **leaves `last_validated` unchanged**; the field only
  refreshes on a genuine content review. Updated all six spots per the CLAUDE.md
  anti-drift rule: `GUIDE.md` §6.3 prose + §6.5 bullet + embedded-template comment
  (→ v4.5, `last_validated` 2026-07-18, Revision History row added),
  `templates/docs/DOC_TEMPLATE.md` comment, and `skills/adaptive-workflow/SKILL.md`
  prose + frontmatter-example comment.
- **Why:** The field was documented to mean two different things; the "every edit"
  reading lets a mechanical frontmatter backfill overstate how recently content was
  validated — a provenance-honesty bug. The fix is portable and inherited by all
  adopters.
- **Scope note:** `SKILL.md` and `DOC_TEMPLATE.md` got **no** doc-standard version
  bump — they live under `source_directories` (`skills`, `templates`), not
  `documentation_directories: ["docs"]`, and carry no `version`/`last_validated`/
  Revision History of their own. Only `GUIDE.md` (which opted into the standard) is
  version-bumped; the source-file edits are traced by this ledger entry. (This
  corrected a reviewer amendment that assumed `SKILL.md` was a governed doc.)
- **Refs:** `GUIDE.md` §6.3/§6.5 (v4.5) · `templates/docs/DOC_TEMPLATE.md` ·
  `skills/adaptive-workflow/SKILL.md` · `.claude/workflow_config.json` ·
  `python -m unittest discover -s tests` (27 pass; no test references `last_validated`)
