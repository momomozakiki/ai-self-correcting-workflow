# 2026-W28  (2026-07-06 – 2026-07-12)

## 2026-07-10

### [code] hooks/workflow_hook.py
- **What:** Implemented the fail-soft hook dispatcher for `SessionStart`,
  `PostToolUse`, and `Stop`: `$CLAUDE_PROJECT_DIR`-first config discovery with a
  walk-up fallback, per-session state (atomic writes, 24h stale purge),
  environment checks honoring `null` version flags, roadmap next-action parsing,
  one-time doc nudge, and bounded Stop reminders (commit + ledger). Stdlib only;
  always exits 0. Supports `--dry-run`.
- **Why:** Executable heart of the v4.1 Adaptive Self-Correcting Workflow design.
- **Refs:** GUIDE.md §7 · plan `implement-the-workflow-design`

### [code] tests/test_hook.py
- **What:** 16 synthetic-event `unittest` tests covering fail-soft, env checks
  (null vs string version flag), source/ledger flag tracking, once-per-session
  doc nudge, Stop block/no-block, `max_blocks` cap, `stop_hook_active`
  short-circuit, and `--dry-run` state isolation. All green.
- **Why:** Guard the dispatcher's contract; keep it dependency-free.
- **Refs:** `python -m unittest discover -s tests`

### [design] GUIDE.md, docs pointer
- **What:** Promoted the v4.1 design doc to canonical `GUIDE.md`; replaced the
  `docs/` copy with a pointer to avoid duplicate drift.
- **Why:** Single source of truth referenced by README, the skill, and the hook.

### [doc] schemas/, skills/, templates/, README/CHANGELOG/ROADMAP/CONTRIBUTING
- **What:** Built out the full `workflow-core` structure (§2.1): config schema
  (+ optional `stop_hook.main_branch`), hook contract, adaptive-workflow SKILL,
  adoption templates, ledger FORMAT, and the workflow's own prose docs.
- **Why:** Make the workflow adoptable by other projects as a submodule.

### [config] .claude/ + CLAUDE.md — self-adoption
- **What:** Wired this repo to run its own hooks (local `hooks/` path, no
  submodule) and keep its own ledger; added the CLAUDE.md fragment.
- **Why:** Dogfood the workflow — practice what we preach.

### [doc] docs/claude-code-hook-integration.md — realign to real dispatcher (v1.4)
- **What:** Rewrote the hook integration guide (v1.3 → v1.4) to describe the
  actual stdlib-Python dispatcher `hooks/workflow_hook.py`, replacing the
  fictional three-file Node.js design. Fixed two hard errors that would break
  adopters: the flat `settings.json` schema (→ nested `matcher`/`hooks[]`/`{type,
  command}`) and `sessionTitle` placement (→ inside `hookSpecificOutput`). Set
  the `PostToolUse` matcher to `Edit|Write|MultiEdit`, documented per-session
  state / `max_blocks` / dirty-tree Stop reminders, preserved the §3.2
  `continue: false` precedence + 8-block cap, switched the deployment checklist
  to Python, and added optional `Bash` git-diff and `PostToolUse` block notes as
  explicitly-not-implemented asides. Added the required `SOURCE-PROVENANCE`.
- **Why:** The doc claimed "APPROVED FOR IMPLEMENTATION" / "validated against
  live sessions" but documented a system that never existed and config that
  silently fails — dangerous for anyone copying it. Realigned to reality.
- **Refs:** `hooks/workflow_hook.py` · `.claude/settings.json` ·
  `schemas/hook_contract.md` · https://code.claude.com/docs/en/hooks ·
  plan `please-check-on-the-robust-bubble`

### [doc] docs/claude-code-hook-integration.md — golden-reference realign (→ v1.7)
- **What:** A user edit (v1.6) had pasted in a different project's content
  (Flutter/Dart monorepo: `packages/*/lib`, `Documentations/`, retrospective
  "Gate 9/10", `dart_flutter_bin`) and reintroduced the invalid flat
  `settings.json` schema. Rewrote to v1.7 so every config key, path, state field,
  and Stop/PostToolUse/SessionStart claim matches this repo's real
  `workflow_hook.py` + `.claude/workflow_config.json`: correct nested settings
  schema, real config keys (`source_directories`/`documentation_directories`/
  `roadmap_file`/`ledger.directory`/`env_check.tool_paths`/`stop_hook.main_branch`),
  real state fields (`source_changed`, `ledger_touched`, `stop_block_count`,
  `doc_nudged`, `session_start_ts`), and the real Stop logic (dirty-non-main +
  ledger, no UNFINISHED/retrospective). Added a §5.3 "Adapting for your repo"
  table + a `$CLAUDE_PROJECT_DIR` explainer so it serves as a golden template;
  unified the version label and provenance; dropped the speculative "Effort
  field" row.
- **Why:** The doc is meant to be accurate for this repo *and* a golden hook
  reference for adopters; v1.6 was neither — it described a foreign project and
  its `settings.json` example would silently fail to register.
- **Refs:** `hooks/workflow_hook.py` · `.claude/workflow_config.json` ·
  `schemas/hook_contract.md` · `python -m unittest discover -s tests` (17 pass)
