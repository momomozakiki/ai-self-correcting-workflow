# Adaptive Self‑Correcting Workflow — moved

The canonical, versioned reference now lives at the repository root:

➡️ **[`GUIDE.md`](../GUIDE.md)**

This file was the original design document (v4.1). It has been promoted to
`GUIDE.md` — the single source of truth referenced by `README.md`, the
`adaptive-workflow` skill, and the hook implementation. Edit `GUIDE.md`, not
this pointer, to avoid the documentation drift the workflow itself is designed
to prevent.
